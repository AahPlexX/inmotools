# Photo Studio — Authoritative Completion Tracker

> This file is the single source of truth for Photo Studio completion state while `feat/photo-studio` is active. Update it whenever a Photo Studio commit changes a gate, remaining task, verification result, integration state, or deployment state. Do not claim completion from the implementation plan alone.

## Deterministic completion goal

Photo Studio is **COMPLETE** only when every gate below is checked on the same final integrated `origin/main` SHA and there are no unresolved Photo Studio release blockers.

- [ ] **Feature contract** — at least 30 genuinely functional, non-destructive editing capabilities are exposed in the production UI and each maps to recipe state plus real preview/export behavior; no inert controls.
- [ ] **Direct editing contract** — local masks and retouch operations can be placed directly on the rendered photo with image-space coordinates that remain correct across supported zoom levels; gestures commit as coherent undo steps.
- [ ] **Geometry/detail contract** — crop/rotation/flip/straighten, lens distortion, horizontal/vertical perspective, texture/clarity/sharpening, luminance/chroma denoise, chromatic-aberration correction, and tone curve are usable and export through the same renderer as preview.
- [ ] **Export contract** — JPEG/PNG/WebP capability probing prevents MIME mislabeling; safe canvas limits are enforced; filename, quality, resize, background, output sharpening, and metadata policy are user-controllable.
- [ ] **Metadata contract** — reviewed metadata can be serialized to XMP; JPEG/PNG/WebP exports attempt standards-compatible embedded XMP where supported; metadata packaging failure never destroys the rendered pixel export; XMP sidecar remains available.
- [ ] **Batch/project contract** — batch processing is sequential and failure-isolated, does not retain all full-resolution outputs in memory, uses the same export policy as single-file export, and recipe JSON import/export round-trips through normalization/version validation.
- [ ] **History/persistence contract** — undo/redo, snapshots/presets/recipe state used by the UI remain deterministic and reversible; stale asynchronous render results cannot replace newer revisions.
- [ ] **Responsive/accessibility contract** — production UI has no page-level horizontal overflow at 320 CSS px, remains keyboard operable for core editing/export flows, exposes meaningful labels/status, and respects reduced-motion behavior.
- [ ] **Focused verification contract** — fresh Photo Studio unit tests, production build, and the complete focused Photo Studio browser matrix pass against the exact final merge state.
- [ ] **Integration contract** — branch is reconciled non-destructively with the then-current `origin/main`; changed-file review confirms no unrelated tool regressions or private/internal prompt leakage; PR merge state is green.
- [ ] **Deployment contract** — the exact integrated `origin/main` SHA is published by GitHub Pages and an uncached live check confirms `/#/tools/photo-studio` loads the intended production workspace.
- [ ] **Tracking closure contract** — this file records the final SHA/evidence, Photo Studio is moved out of `.tasks/IN_PROGRESS.md`, closure is recorded in `.tasks/DONE.md`/`.tasks/WORK_LOG.md`, and any intentionally deferred decoder enhancement is explicitly recorded rather than silently omitted.

If any box above is unchecked, Photo Studio is not complete.

## Current state

- Status: **IN PROGRESS**
- Working branch: `feat/photo-studio`
- Pull request: `#29`
- Working branch for continuation (2026-09-24): `claude/photo-tool-completion-ama1g1`, created from `feat/photo-studio` at `1e74011d` so all Photo history stays in one lineage. `feat/photo-studio` / PR `#29` were not modified; when the continuation branch merges into `main`, PR `#29`'s commits are contained in it.
- Current phase: **Phases 1 and 3–6 are complete, and all 164 ledger capabilities are implemented and browser-verified (2026-09-25). Remaining: Phase 7 only (audit, notices link, integration with `main`, Pages proof, tracker closure).**
- Current verification state (2026-09-25, integrated with `main`): PR #78 head `873f29f` passes the repository's full Pages workflow on the PR merge state (run 36192897759: unit tests, production build, and the complete desktop + mobile Chromium browser suite, all green). `main` was merged in at `c290290` and again at `a1963e4`; the five browser-suite failures inherited from `main` and the flakes CI retries were absorbing were root-caused and fixed on this PR (see `.tasks/NEXT.md`, TASK-014 2026-09-25 continuation). Remaining gates: merge to `main`, Pages deployment proof, tracker closure.
- Previous verification state (2026-09-25, scattered capabilities): Photo unit suite plus selector **41 files / 445 tests** pass; `tsc` clean; production build clean; focused Photo browser matrix **210/210** (the 9 prior specs, 184, plus `photo-editing-extras.spec.ts`, 13 cases × desktop/mobile) passes on desktop and mobile Chromium. Phase 7 audit so far: an axe scan of every panel with every section expanded reports no serious or critical violations (it caught and fixed a dangling `aria-controls` on the guides toggle), and every panel reflows at 320 CSS px without horizontal scrolling. Escape now disarms any active canvas tool (brush, picker, crop, straighten), and the tool hints say so.
- Previous verification state (2026-09-24, Phase 6): Photo unit suite plus selector **37 files / 419 tests** pass; `tsc` clean; production build emits the AVIF encoder (`assets/avif-encode.worker-*.js` 20 kB, `assets/avif_enc-*.wasm` 3.49 MB) outside precache with the `photo-avif-encoder` runtime rule; the full 9-file focused Photo browser matrix **184/184** passes on desktop and mobile Chromium.
- Previous verification state (2026-09-24, Tasks 5.2–5.4): full Photo-prefixed unit suite plus selector **31 files / 366 tests** pass; `tsc` clean; production build emits `assets/photo-merge.worker-*.js` (15.59 MB) outside precache (136 entries / 14,410.44 KiB; `sw.js` references the engine only through the `photo-merge-engine` runtime rule); the full 8-file focused Photo browser matrix **156/156** passes on desktop and mobile Chromium with two workers.
- Local browser runs in the hosted container use an untracked Playwright override (`playwright.local.config.ts`, listed in `.git/info/exclude`) that sets `launchOptions.executablePath` to the preinstalled Chromium, because the pinned Playwright expects a browser build the container does not ship. CI uses the committed `playwright.config.ts` unchanged.
- Latest inspected branch/PR context (2026-09-17): PR `#29` remains open/draft and reports `DIRTY` against the moving base; its inspected remote head before this runtime/documentation batch was `1ef0c8a3f3b497facd823ab18ce0151d5b0a266d`. The local Photo runtime head is `8934f1c9771f087a4008787908e27bdc319a0464`; latest fetched `origin/main` is `b27cf8cbc51c38a1942c2ba9410d9b37879745f7` (`docs(markdown): record audit verification and remaining scope`), an unrelated workstream commit. Main was not merged, rebased, rewritten, or pushed by this Photo batch. Local exact-head GREEN is not an integrated-release claim.
- Test-count correction: the earlier recorded 110-case Photo unit/selector total was not the exact Photo namespace count. PR run `34974201415` establishes 93 before TIFF; TIFF adds 28, yielding 121; RAW acquisition adds 20, yielding 141; RAW development adds 17, yielding 158; embedded previews add 15, yielding 173; Task 2.1 direct crop/straighten geometry adds 8, yielding 181; Task 2.2 advanced tone/color adds 4, yielding 185; Task 2.3 deterministic analysis adds 3, yielding 188; Task 2.4 LUT workflow adds 5, yielding 193; Task 2.5 ICC color management adds 8, yielding 201; Task 2.6 inspection scopes adds 5, yielding 206; Task 3.1 selection adds 8, yielding the current tracked total of 214. No tracked tests were removed. For a clean checkout, run the Photo-prefixed unit files plus `tests/unit/e2e-spec-selection.test.mjs` sequentially with Vitest and `--maxWorkers=1`.
- Workspace ownership note (2026-09-17): the local worktree contains a separately owned, unstaged `tests/unit/photo-raw.test.ts` exposure-baseline RED change. It is intentionally excluded from `8934f1c9771f087a4008787908e27bdc319a0464` and from the 214-test tracked-branch total. A fresh run of that working-tree file produced 45 maintained passes plus the expected failure of the separately owned exposure assertion because the binding returns identical samples for neutral and +1 EV. Do not discard, stage, or rewrite this file without first reconciling ownership; it describes the still-open LibRaw exposure blocker rather than implemented behavior.
- Prior PR workflow context: run `34726437963` passed all 722 repository unit tests and the production build at earlier Photo head `93a3535252c843cc27146030373e377b9e35f12a`, then failed the expanded 502-case repository browser suite. Its two Photo-specific failures were test-contract defects (an unscoped page-level status locator and mouse-only range dragging under touch emulation), both corrected at `5612412d8e0416f62f14bd82d351d4c76ac77966`; unrelated tool failures remain outside Photo Studio scope.
- Current verified merge-state evidence: PR workflow run `34706122558` completed successfully for earlier Photo head `5394c88b560af6ab5933968771334502da9c207e`; the repository unit suite, production build, and the complete focused Photo Studio browser matrix all passed on that PR merge state.
- Most recent intentional RED evidence: workflow run `34706499377` for head `8682b9a5b241c6d9ec9899f722a0b31de3ab7ed6` passed repository unit tests and the production build, then failed browser validation at the new copy/paste contract because the production `Copy edits` / `Paste edits` behavior did not yet exist.
- Task 1.1 local RED evidence: the import unit/selector contract first failed because `photo-import.ts` and focused spec selection did not exist; browser contracts then failed for the missing drop target and clipboard controls. Independent review regressions additionally reproduced stale clipboard replacement, outside-root paste loss, premature URL revocation, inaccessible hidden-input focus, misleading extension fallback, and preview-status races before their fixes.
- Historical exact-head CI evidence: workflow run `34662031647` completed successfully for `735f9f915cf8427c95444d3296ce065e688a2ea0`; all 661 repository unit tests, production build, and all 24 focused Photo Studio browser tests succeeded on that earlier PR merge state.

## Verified progress

- [x] Core typed `PhotoRecipe`, normalization, history, histogram, metadata serialization, and non-destructive renderer architecture exist.
- [x] Revision-aware preview/export rendering and stale-result rejection exist.
- [x] Direct on-image radial/local and retouch placement is implemented; pointer coordinates are normalized to the visually rendered image bounds.
- [x] Zoom interaction regression coverage includes above-100% zoom placement.
- [x] Lens distortion and horizontal/vertical perspective transforms are implemented in the shared geometry path.
- [x] Texture, clarity, sharpening, luminance/chroma denoise, and chromatic-aberration correction exist in the pixel engine.
- [x] Tone-curve editing is wired into normal recipe history and preview/export behavior with accessible graph + numeric editing.
- [x] Embedded-XMP container writer has focused unit coverage for JPEG APP1, PNG XMP `iTXt`, and WebP `XMP ` / VP8X behavior.
- [x] Single-photo export uses unified export orchestration with safe editable filename, resize policy, output sharpening, metadata policy, standards-compatible XMP embedding attempt, and pixel-export-preserving fallback.
- [x] Batch export is exposed in the production dialog, processes sequentially with per-file status, shares format/resize/sharpening/metadata policy, and does not retain completed full-resolution blobs in the batch summary.
- [x] Recipe JSON export/import, editable presets, history, and snapshot restore paths remain wired.
- [x] Every exposed scalar adjustment has an individual reset that restores its actual neutral/default value without resetting neighboring controls.
- [x] Snapshots accept user-provided names and restore the captured normalized recipe through ordinary history.
- [x] Validated custom crop-ratio inputs apply a centered exact-ratio crop through the same normalized recipe path as built-in presets.
- [x] RGB + luminance histogram is presented in the live preview toolbar with four distinct channels.
- [x] Highlight/shadow clipping warnings render as a zoom-synchronized non-destructive overlay computed from the actual rendered preview pixels.
- [x] Color sampler reads the rendered preview in intrinsic image coordinates and reports hexadecimal, RGB, HSL, and alpha when relevant.
- [x] Long-edge and short-edge export sizing preserve the edited aspect ratio for landscape and portrait frames.
- [x] The export dialog exposes planned dimensions before render and blocks known-oversized single exports until the user explicitly selects the offered verified-safe dimensions.
- [x] Safe export planning honors both edge and area ceilings and preserves aspect ratio.
- [x] Before/after comparison now supports an explicit split mode with a pointer-draggable, keyboard-operable split slider plus a side-by-side mode; both surfaces share synchronized zoom and preserve image-space registration for overlays and direct editing.
- [x] Photo source changes select the full focused Photo browser matrix (`photo.spec.ts`, `photo-controls.spec.ts`, `photo-compare.spec.ts`, `photo-copy-paste.spec.ts`, `photo-import.spec.ts`, `photo-project.spec.ts`, and `photo-geometry.spec.ts`) rather than silently omitting specialized Photo specs.
- [x] Browser coverage verifies tone-curve undo semantics, direct mask/retouch placement, reviewed XMP sidecar, actual embedded XMP in PNG, safe custom filename, output sharpening, production batch queue completion, RGB histogram, clipping overlay, color sampler, individual resets, named snapshots, exact custom crop ratio, long-/short-edge sizing, explicit safe-size selection, keyboard history, synchronized split/side-by-side comparison, and 320 CSS px editor/export-dialog reflow.
- [x] Exact Photo head `5394c88b560af6ab5933968771334502da9c207e` passed repository unit tests, production build, and the full focused Photo browser matrix through PR validation run `34706122558`.
- [x] In-session copy/paste implementation exists at `93a3535252c843cc27146030373e377b9e35f12a`: Copy stores an independent normalized recipe snapshot, source changes preserve the in-session clipboard, and Paste routes through one normal history commit. Its focused desktop/mobile browser contract is GREEN at `5612412d8e0416f62f14bd82d351d4c76ac77966`.
- [x] Unsupported encoders are rejected deterministically: capability probing accepts a format only when the produced `Blob.type` exactly matches the requested MIME, unsupported format options are disabled in the export UI, and render-time MIME mismatch throws for export rather than mislabelling fallback bytes.
- [x] Stale renders are rejected deterministically: every preview request carries a monotonically increasing revision, result and error handlers compare it with the active revision before mutating preview/status state, and `tests/unit/photo.test.ts` proves older revisions fail `isRenderResultCurrent` while the active revision passes.
- [x] Drag/drop, direct clipboard read, body-level image paste, mobile camera capture, and ordinary file input route through one local import contract at `77d00cf5ea7b78a397fbe24c94453137456e8678`; unsupported/denied clipboard paths remain truthful and preserve the active document.
- [x] Import operations are revision-gated from operation start, source URL replacement is transactional, stale clipboard work cannot decode over a newer source, older preview success/failure cannot replace newer import guidance, and source swaps clear prior preview observation state before the new render is ready.
- [x] Concurrent preview/export worker requests use internal unique correlation IDs independent of caller revision counters, with deterministic reverse-order fake-worker proof at `f057e8e`.
- [x] Durable local projects and recovery are implemented at `448f46e695cef35abfeb46ab6a4c5095d6c1be6e`: versioned/migrated IndexedDB metadata, OPFS source storage with IndexedDB fallback, 800 ms autosave, explicit save/load/delete, startup recovery, quota/capability/durability reporting, conservative orphan cleanup, unique replacement keys, browser-wide mutation coordination, and stale-action/save-state guards. Final evidence is 76 Photo unit/selector tests, the production/PWA build, and 66 desktop/mobile browser cases.
- [x] Virtual copies and local user presets are implemented at `0621d34d3a8dd3513ab799ee960666bed6e1254a`: copies share one immutable persisted source while retaining independent normalized histories/snapshots; deletion and replacement preserve referenced bytes; presets are durable, named, editable, normalized, versioned for import/export, and removable. Historical evidence includes unit/build verification, 70 desktop/mobile browser cases, and a clean Astral closure review after both identified P1 races were repaired; the unit-count correction above supersedes the previously recorded 110-case scoped total.
- [x] Capability-gated TIFF acquisition is implemented at `fc89cb5f414ce1a2955188a19252ae19f3b58dba` with exact `tiff@7.1.3`, bounded first-page raster normalization, unsigned 8/16-bit sample intermediates, immutable original source storage, sequential deadline-limited decoder workers, shared preview/export semantics, and shipped dependency license notices. Unsupported variants and the 8-bit editing/export boundary are explicit rather than silently misdecoded.
- [x] RAW acquisition at `5d43c20e811145d2c2b05ba7fc79ac3d62b890f6` now has source-gated development and inspection at `601d982c0a0d3081e1509b3dd702ff7e74dc58ed`: camera/daylight/custom RGB white balance, clip/unclip/blend highlight handling, practical Bayer interpolation choices, read-only camera/source facts, normalized independent recipe history, durable recovery, and shared preview/single/batch rendering. This remains partial capability 8 because exposure baseline is open.
- [x] Embedded RAW previews (capability 7) are implemented at `71831b6d2ae7b8e53e454d829c1fb2b3442dd799`: bounded JPEG/RGB thumbnail extraction before sensor unpack, native orientation-aware rasterization, clearly non-editable interim viewing, truthful full-decode fallback, shared queue/cache progress, and revision-gated URL cleanup without replacing the active document or export source.

- [x] Phase 2 / Task 2.1 direct composition geometry is reconciled through `eb67928664087c5c6b143cab5812ab7cac00511c`: direct crop move/edge/corner manipulation, constant 32 CSS px pointer targets, keyboard/numeric parity, configurable grid plus thirds/diagonal/exact golden-ratio overlays, direct straighten tracing, stale-gesture cancellation on source replacement, and preview/export/recovery pixel parity. Crop/straighten gestures commit once to ordinary normalized recipe history; the final 122-case desktop/mobile Photo matrix is GREEN with one worker.
- [x] Phase 2 / Task 2.2 advanced tone/color is implemented through `eb67928664087c5c6b143cab5812ab7cac00511c`: independent red/green/blue curves, normalized input/output levels with gamma, and a three-output channel mixer are reversible recipe state, cloned by history/persistence, rendered by the shared preview/export engine, exposed with labeled controls, and covered by pixel/normalization unit contracts plus desktop/mobile UI undo contracts.
- [x] Phase 2 / Task 2.3 deterministic automatic adjustments are implemented at `021d5bc9b24f242ddcc57f4a0591e8331e1f8321`: auto tone writes bounded exposure/contrast/highlight/shadow/white/black values from fixed neutral-source luminance percentiles; auto white balance writes bounded temperature/tint values from fixed gray-world channel means. Both expose their numeric output, remain editable/resettable, undo in one step, reuse one cached neutral histogram per source, and have deterministic fixture plus desktop/mobile browser evidence. These are transparent local suggestions, not ML, scene recognition, ICC-aware adaptation, or a claim of objective color accuracy.
- [x] Phase 2 / Task 2.4 LUT workflow is implemented at `f042433d98e2538e7e9c36d9058a9f8c4648165b`: common 3D `.cube` files import into optional normalized version-1 recipe state, apply red-fastest trilinear interpolation in the shared preview/single-export/batch engine after levels/curves/channel mixing and before vignette/grain, expose 0–100% strength, preserve the prior recipe on malformed import, participate in ordinary history/project/preset/recipe persistence, and export the active LUT with strength baked into the output grid. The UI explicitly states that unrelated Photo adjustments are not represented by LUT export.
- [x] Phase 2 / Task 2.5 ICC color management is implemented at `24ecb4b2162d11cae8217dccc0e82ace73a429c7`: bounded RGB/CMYK/gray ICC import feeds a standards-based LittleCMS transform path; source assignment remains distinct from output conversion; preview-only proofing supports intent, black-point compensation, output-gamut warning, and before/after proof sampling; single and batch export share the conversion pipeline and require standards-compatible profile embedding in JPEG, PNG, or WebP.
- [x] Phase 2 / Task 2.6 inspection scopes is implemented at `408b4f07525ec43fe11f3ae83da8f23a32276dc5`: the current rendered preview feeds bounded waveform, RGB parade, YCbCr vectorscope, and exposure-zone analysis; deterministic focus/detail and exposure overlays align with the preview; up to eight normalized pinned samples retain coordinates and refresh values across edits; active color management enables D65 XYZ/Lab readout; and the canvas exposes a zoom navigator plus configurable checkerboard/solid backgrounds.
- [x] Phase 3 / Task 3.1 selection model is implemented at `8934f1c9771f087a4008787908e27bdc319a0464`: bounded normalized selection operations cover rectangle, ellipse, polygon/lasso, rendered-color tolerance, luminance range, replace/add/subtract/intersect, feather, grow/shrink, inversion, clear, keyboard-operable defaults, direct canvas gestures, and one-step conversion into a shared-renderer local mask.

## Original spec inventory blockers discovered during audit

These remain release blockers because they are promised by the design specification but are not yet fully represented in the production UI/behavior or final evidence:

- [x] RGB histogram presentation.
- [x] Clipping-warning overlay/toggle for highlight/shadow clipping.
- [x] Color sampler with RGB, HSL, and hexadecimal readout.
- [x] Individual-control reset affordances rather than reset-all only.
- [x] Named snapshots.
- [x] Explicit long-edge and short-edge export resize modes.
- [x] Explicit safe-size export choice/warning before an oversized render.
- [x] True validated custom crop ratio.
- [x] Draggable and side-by-side before/after comparison.
- [x] In-session copy/paste recipe semantics across sources as one undo step — implementation at `93a3535252c843cc27146030373e377b9e35f12a`; exact focused local GREEN evidence at `5612412d8e0416f62f14bd82d351d4c76ac77966`.
- [x] Explicit unsupported-encoder and stale-render accounting — exact MIME verification at capability-probe and export boundaries plus revision-gated preview state and focused unit evidence; no weaker timing-sensitive browser race was added.

## Capability expansion execution

- [x] Phase 0 / Task 0.1 — individual scalar resets.
- [x] Phase 0 / Task 0.2 — validated custom crop ratio, implemented at `b3fb5239dbd8910fd0edbb3071d9cf482bc9fa7f` with exact 5:4-on-4:3 desktop/mobile browser evidence.
- [x] Phase 0 / Task 0.3 — draggable and side-by-side comparison, implemented at `5394c88b560af6ab5933968771334502da9c207e` with strict RED evidence in run `34705869311` and GREEN merge-state evidence in run `34706122558`.
- [x] Phase 0 / Task 0.4 — in-session copy/paste edits; implementation committed at `93a3535252c843cc27146030373e377b9e35f12a`, with all 42 focused Photo browser cases GREEN at `5612412d8e0416f62f14bd82d351d4c76ac77966`.
- [x] Phase 0 / Task 0.5 — final base-contract evidence and explicit unsupported-encoder/stale-render accounting recorded from deterministic runtime and unit evidence at `5612412d8e0416f62f14bd82d351d4c76ac77966`.
- [x] Phase 1 / Task 1.1 — failure-safe drag/drop, clipboard, camera, and native raster acquisition, implemented at `77d00cf5ea7b78a397fbe24c94453137456e8678` with 20 desktop/mobile import browser cases plus focused unit coverage.
- [x] Phase 1 / Task 1.2 — durable projects/recovery, implemented at `448f46e695cef35abfeb46ab6a4c5095d6c1be6e` with final unit/build/browser evidence and clean Astral closure review.
- [x] Phase 1 / Task 1.3 — virtual copies and user presets, implemented at `0621d34d3a8dd3513ab799ee960666bed6e1254a` with final unit/build/browser evidence and clean Astral closure review.
- [x] Phase 1 / Task 1.4 — capability-gated TIFF adapter, implemented at `fc89cb5f414ce1a2955188a19252ae19f3b58dba` with 24 TIFF unit cases, shared-import regressions, production/PWA evidence, and the complete 78-case Photo browser gate.
- [x] Phase 1 / Task 1.5 — RAW acquisition implemented at `5d43c20e811145d2c2b05ba7fc79ac3d62b890f6`; supported white-balance/highlight/Bayer demosaic controls and camera/source inspection implemented at `601d982c0a0d3081e1509b3dd702ff7e74dc58ed`; embedded previews implemented at `71831b6d2ae7b8e53e454d829c1fb2b3442dd799`. RAW exposure baseline (LibRaw pre-demosaic exposure correction) implemented 2026-09-25; see the scattered-capabilities entry below.
- [x] Phase 2 / Task 2.1 — direct crop handles and composition overlays, implemented through `eb67928664087c5c6b143cab5812ab7cac00511c` with direct crop move/resize, configurable grid plus thirds/diagonal/exact golden-ratio overlays, source-frame straighten tracing, numeric/keyboard/touch parity, focused unit contracts, and 122/122 final desktop/mobile browser evidence.
- [x] Phase 2 / Task 2.2 — per-channel curves, levels, and channel mixer, implemented through `eb67928664087c5c6b143cab5812ab7cac00511c` in the normalized reversible recipe and shared preview/export renderer with focused pixel, normalization, UI, and undo evidence.
- [x] Phase 2 / Task 2.3 — deterministic auto tone and white balance, implemented at `021d5bc9b24f242ddcc57f4a0591e8331e1f8321` with visible bounded recipe output, one-step undo, neutral-source histogram reuse, stale-result guards, deterministic unit fixtures, and 124/124 final desktop/mobile browser evidence.
- [x] Phase 2 / Task 2.4 — bounded 3D Cube LUT import/application, strength, and supported export, implemented at `f042433d98e2538e7e9c36d9058a9f8c4648165b` with parser/interpolation/domain/strength/export-round-trip unit evidence, desktop/mobile import/history/error/download coverage, a production/PWA build, and the browser evidence described in Current state.
- [x] Phase 2 / Task 2.5 — standards-based ICC transforms and soft proof, implemented at `24ecb4b2162d11cae8217dccc0e82ace73a429c7` with exact `lcms-wasm@1.0.5`, bounded profile parsing, explicit assign/convert semantics, proof intent/BPC/gamut controls, proof-aware sampling, mandatory output-profile embedding, focused transform/container fixtures, a production/PWA build, clean production audit, and 128/128 final desktop/mobile Photo browser evidence.
- [x] Phase 2 / Task 2.6 — inspection scopes, overlays, pinned coordinate/color sampling, zoom navigator, and viewing backgrounds, implemented at `408b4f07525ec43fe11f3ae83da8f23a32276dc5` with deterministic mapping/overlay fixtures, production/PWA build, and 130/130 final desktop/mobile Photo browser evidence.
- [x] Phase 3 / Task 3.1 — selection model, implemented at `8934f1c9771f087a4008787908e27bdc319a0464` with deterministic geometry/pixel/composition fixtures, legacy-recipe normalization, shared preview/export mask behavior, production/PWA build, and 132/132 final desktop/mobile Photo browser evidence.
- [x] Phase 3 / Task 3.2 — mask management (rename, duplicate, delete, visibility, overlay visualization, overlay appearance, add/subtract/intersect composition) was found already implemented and tested during a 2026-09-21 audit of this ledger against the actual codebase; see the correction note below.
- [x] Phase 3 / Task 3.3 — professional brush controls, implemented 2026-09-21: flow (per-pass coverage ceiling with cross-pass "over" accumulation so repeated passes build toward full coverage without a single pass exceeding it), spacing (minimum-distance dab decimation as a fraction of radius), stroke smoothing (interior-only exponential moving average that keeps a gesture's first and last points exact), and a dedicated erase mode (per-stroke, multiplicatively removes coverage a paint stroke added, never adds where nothing was painted). Brush hardness is the existing shared `feather` control (same concept, same slider, already reachable for brush masks). Pointer-pressure response already existed (`PointerEvent.pressure`, degrading to a constant 1 on devices that report 0) and was preserved unchanged.
  - Legacy compatibility: points saved before this change lack a `strokeId`; normalization defaults them to one shared pass (`strokeId: 0`), which reproduces the exact prior max-based weight for every existing saved brush mask regardless of stroke count — verified by a dedicated regression test.
  - Files: `photo-types.ts` (`PhotoBrushPoint`, brush mask `flow`/`spacing`/`smoothing` fields), `photo-engine.ts` (`brushWeight` rewritten for per-stroke-then-cross-stroke accumulation, `normalizeMask` defaults/clamps), `photo-interaction.ts` (`smoothBrushPath`, `decimateBrushPath`, `applyLocalGesture` gained `strokeId`/`erase` parameters), `PhotoWorkspace.tsx` (Flow/Spacing/Smoothing controls, erase-mode toggle, stroke-id counter), `PhotoCanvas.tsx`/`photo.css` (erase dabs render distinctly via `data-photo-brush-dab`).
  - Verification: `tsc` clean; 9 new unit tests in `tests/unit/photo-brush-controls.test.ts` (flow capping, cross-pass buildup, legacy single-pass regression, erase subtraction, spacing decimation, smoothing) plus one existing test updated for the extended point shape — all pass; a new e2e case (`photo.spec.ts` — "brush masks expose flow, spacing, and smoothing controls and record erase strokes separately") passes on desktop+mobile Chromium; the full focused Photo browser matrix (134 cases across all 7 Photo spec files) passes with zero regressions; production build clean.
- [x] Phase 3 / Task 3.4 — retouch workflow depth, implemented 2026-09-21: multi-stroke clone/heal (capability 100) painted as a continuous drag or repeated strokes, all sharing one source offset locked from the first source/target placement (capability 99's locking half — visualization already existed as the source/target line and circles) until the source is re-anchored; per-operation bypass/visibility (capability 101, an `enabled` toggle matching local adjustments); and operation reordering (move up/down) as an added UX capability beyond the numbered list.
  - Design: the offset is computed once from the source/target anchor and reused for every later stroke point, so dragging paints a coherent stroke that samples a correspondingly-shifted source region — real clone-stamp "aligned" behavior, not each dab re-deriving its own offset. An `anchored` flag (not stroke-path emptiness, which a same-position click's `[start, end]` gesture path can leave misleadingly non-empty) is the authoritative signal for "has this operation's target ever been explicitly placed"; re-placing the source resets it.
  - Legacy compatibility: operations saved before this change have no `path`/`anchored`/`enabled` fields; normalization defaults them to `path: []`, `anchored: true`, `enabled: true` — rendering every existing saved retouch operation exactly as the single anchored stamp it always was.
  - Files: `photo-types.ts` (`path`/`anchored`/`enabled` on clone/heal, `enabled` on red-eye), `photo-engine.ts` (`applyCloneOrHeal` unions weight across the anchor plus all path stamps while sampling with one locked delta; `applyRetouch` skips disabled operations; `normalizeRetouch` defaults), `photo-interaction.ts` (`placeRetouchPoint` gained a `path` parameter and the anchor/append branching), `PhotoWorkspace.tsx` (Enabled checkbox, Move up/down, Clear stroke, target painting stays active across strokes like brush painting), `PhotoCanvas.tsx`/`photo.css` (path stamps render as additional stroke circles; disabled operations hide their overlay).
  - Verification: `tsc` clean; 7 new unit tests in `tests/unit/photo-retouch-strokes.test.ts` (anchor-on-first-placement, append-on-later-strokes, re-source clears the stroke, legacy normalization, a pixel-level test proving a stroke point away from the anchor samples the source shifted by the same locked delta, and bypass skipping pixel work entirely) plus the full existing Photo unit suite (24 files, 236 tests) — all pass; a new e2e case (`photo.spec.ts` — "retouch operations paint multi-stroke coverage, bypass, reorder, and clear independently") passes on desktop+mobile Chromium; the full focused Photo browser matrix (138 cases across all 7 Photo spec files) passes with zero regressions; production build clean.
  - Phase 3 is now fully complete (Tasks 3.1–3.4). Phases 4–7 (layers/compositing, warp/liquify and deterministic filters, multi-image processing, workflow/export depth, final audit/integration/deployment) remain open.
- [x] Phase 4 / Task 4.1 — layer document model and compositing engine, implemented 2026-09-21: `PhotoLayer` (id, name, visible, opacity, blend mode, transform, optional mask, embedded `sourceDataUrl`/dimensions — self-contained like `PhotoLut.data`, so a recipe stays one portable JSON document) added to the recipe; a pure, synchronous compositing renderer in `photo-engine.ts` applies each visible layer's transform (bilinear-sampled translate/scale/rotate), its mask (reusing the shared `photoMaskWeight` evaluator, not a duplicate path), and all 12 required blend modes (normal, multiply, screen, overlay, soft-light, hard-light, darken, lighten, color, luminosity, hue, saturation — the last four via the standard W3C non-separable Lum/Sat/SetLum/SetSat compositing formulas) on top of the fully-adjusted base image, as the final pipeline step.
  - **No capability credit taken for this task.** This is infrastructure only — no UI exists yet to place, adjust, or reorder a layer, and per this tracker's own completion contract infrastructure alone never increments the capability count. Capabilities 112–122 (layer import/rename/reorder/visibility/opacity/blend-modes/masks/adjustment-layers/transform/duplicate/delete) become creditable only once Task 4.2 (Layer UI) wires this engine into the actual workspace.
  - Design boundary: layer image decoding (base64 → pixels) is inherently async, while this whole pixel pipeline is synchronous by architecture (matching how the primary source image is already decoded before entering it). `applyPixelAdjustments` therefore takes an optional `PhotoLayerPixels[]` of pre-decoded buffers keyed by layer id; a layer with no matching entry composites as a no-op rather than throwing, so Task 4.2 can decode layers asynchronously (reusing the existing import/canvas-decode utilities) without this engine needing to change.
  - Legacy compatibility: recipes saved before this change have no `layers` field; normalization defaults it to `[]`, so `isFullyNeutral`'s fast path and every existing recipe's rendered output are unchanged. Verified: `normalizeRecipe` on a pre-existing recipe shape produces `layers: []` and this is covered by a dedicated regression test.
  - Files: `photo-types.ts` (`PhotoLayer`, `PhotoBlendMode`, `PhotoLayerTransform`, `layers?` on `PhotoRecipe`), `photo-layers.ts` (new — blend math, `createLayer`, `cloneLayer`, `normalizeLayerFields`), `photo-engine.ts` (`normalizeLayer`, `cloneRecipe`/`normalizeRecipe`/`DEFAULT_RECIPE` wiring, `bilinearSample`/`compositeOneLayer`/`compositeLayers`, `applyPixelAdjustments` gained the optional `layerPixels` parameter and the `isFullyNeutral` fast-path fix).
  - Verification: `tsc` clean; 14 new unit tests in `tests/unit/photo-layers.test.ts` (blend-mode math for 6 representative modes including 2 non-separable ones, field normalization/clamping, legacy-recipe regression, 50-layer cap, and 5 compositing behaviors — centered placement, invisible-layer no-op, undecoded-layer no-op, partial opacity, mask restriction) plus the full existing Photo unit suite (26 files, 250 tests) — all pass; production build clean; the full focused Photo browser matrix (138 cases across all 7 Photo spec files) passes with zero regressions, confirming this pure-engine addition changes nothing observable until a UI uses it.
- [x] Phase 4 / Task 4.2 — Layer UI, implemented 2026-09-21: a new Layers panel/tab makes the Task 4.1 engine reachable, crediting capabilities 112 (image layer import from a local file), 113 (rename), 114 (reorder — Move up/down), 115 (visibility), 116 (opacity), 117 (all 12 blend modes via a `<select>`), 118 (radial/linear/brush mask assignment), 120 (non-destructive position/scale/rotation transform), 121 (duplicate), and 122 (delete). Capability 119 (adjustment layers) is explicitly Task 4.3's scope, not this one. Capability 123 (layer grouping) is explicitly conditional in the plan ("if the implemented stack complexity justifies it") and is deferred — nothing in this Layer UI yet creates the kind of deep stack that would need it.
  - Layer masks reuse the exact same gesture-placement logic as local-adjustment masks rather than a parallel implementation: a new `applyLayerMaskGesture` in `photo-interaction.ts` mirrors `applyLocalGesture`'s radial/linear/brush cases but targets a `PhotoLayer`'s own mask, and a shared `maskShape()` helper in `PhotoCanvas.tsx` now renders both local-adjustment and layer mask edit-overlays (previously local-adjustment-only logic, factored out rather than duplicated for layers).
  - Render pipeline: layer image decoding is inherently async (the whole pixel pipeline is synchronous by design), so `renderPhoto()` now decodes each layer's embedded `sourceDataUrl` via `createImageBitmap` before compositing and threads the decoded `PhotoLayerPixels` through `processPixels()` to both the Worker path (`photo.worker.ts`, transferring its own copy of each buffer so a worker failure can still fall back to the main thread using the caller's original, untransferred buffers — mirroring the existing pattern for the main pixel buffer) and the main-thread fallback (`processPhotoColorPipeline`). `PhotoWorkspace.tsx` never manages decoding directly; it only ever edits `recipe.layers`.
  - `addImageLayer` reads `recipe.layers` fresh inside the `setHistory` updater at decode-completion time rather than from the recipe captured when the file picker fired, so another edit made during the async decode is never silently discarded — the same staleness class of bug the Task 3.4 correction avoided for retouch strokes.
  - Files: `photo-import.ts` (`decodeImageFileForLayer`), `photo-renderer.ts` (`decodeLayerPixels`, `processPixels`/`renderPhoto` threading), `photo.worker.ts` (`ProcessMessage` gained `layers`), `color/photo-color-pipeline.ts` (`processPhotoColorPipeline` gained `layerPixels`), `photo-interaction.ts` (`applyLayerMaskGesture`), `PhotoCanvas.tsx` (`layers` prop, shared `maskShape()`), `PhotoWorkspace.tsx` (Layers panel/tab, `addImageLayer`/`updateLayer`/`removeLayer`/`duplicateLayer`/`reorderLayer`/`setLayerMaskType`, `'layer-mask'` canvas-interaction handling).
  - Verification: `tsc` clean; full existing Photo unit suite (25 files, 250 tests) unaffected and green; 1 new e2e case (`photo.spec.ts` — "layers import, blend, transform, mask, duplicate, reorder, and remove independently") passes on desktop+mobile Chromium; the full focused Photo browser matrix (140 cases across all 7 Photo spec files) passes with zero regressions; production build clean.
  - Remaining Phase 4 work: Task 4.3 (adjustment/text/watermark/shape layers — capabilities 119, 124–126) and Task 4.4 (warp/liquify and deterministic filters — capabilities 102–108). Phases 5–7 (multi-image processing, workflow/export depth, final audit/integration/deployment) remain open.
- [x] Phase 4 / Task 4.3 — adjustment, text, watermark, and shape layers, implemented 2026-09-22: `PhotoLayer` gained a `role` discriminator (`'image' | 'adjustment' | 'text' | 'shape'`, defaulting to `'image'` for every pre-existing layer) plus role-specific fields (`effect` for adjustment; `text`/`textColor`/`fontSize` for text; `shapeKind`/`shapeColor`/`shapeStrokeWidth`/`shapeFilled` for shape; `isWatermark` marking an image layer added via the watermark quick-action). Four factories (`createImageLayer`, `createAdjustmentLayer`, `createTextLayer`, `createShapeLayer`) replace the single old `createLayer`, crediting capabilities 119 (adjustment layers), 124 (text layer), 125 (image watermark/logo layer, modeled as an `isWatermark`-flagged image layer with anchor-preset positioning rather than a separate role, since it is the same content type just placed via presets), and 126 (basic rectangle/ellipse/line shape overlays for proofing/annotation).
  - Design: 'image', 'text', and 'shape' roles all still flow through the existing, unchanged `compositeOneLayer` transform/blend/mask math — text and shape layers just produce their `PhotoLayerPixels` buffer by rendering to an offscreen canvas at decode time (`renderTextLayerPixels`/`renderShapeLayerPixels` in `photo-renderer.ts`) instead of decoding a stored `sourceDataUrl`, so no compositing code needed to change. 'adjustment' is the one architecturally different role: it has no pixel buffer of its own; `applyAdjustmentLayer` in `photo-engine.ts` scales its `LocalEffect` by the layer's opacity and applies it directly to the pixels beneath it via the same `applyLocalColorEffect`/`applyLocalSpatialEffect` functions local adjustments already use (refactored to take `(mask, effect)` directly rather than a whole `LocalAdjustment`, so both call sites share the exact math), gated by the layer's own mask or a full-canvas fallback mask when none is set.
  - Legacy compatibility: layers saved before this change have no `role` field; normalization defaults it to `'image'` (the only role that existed before), and a `fontSize: 0` on a saved layer is preserved (clamped to the minimum) rather than silently overridden by the `48` fallback — a `clampRange` helper using `Number.isFinite` replaced an earlier `... || 48` expression that would have collided with legitimate zero values.
  - Files: `photo-types.ts` (`PhotoLayerRole`, `PhotoShapeKind`, new `PhotoLayer` fields), `photo-layers.ts` (`clampRange`, `baseLayer`, the four role factories, extended `normalizeLayerFields`), `photo-engine.ts` (`applyLocalColorEffect`/`applyLocalSpatialEffect` signature refactor, `FULL_CANVAS_LAYER_MASK`, `applyAdjustmentLayer`, `compositeLayers` role branching), `photo-renderer.ts` (`renderTextLayerPixels`, `renderShapeLayerPixels`, `decodeLayerPixels` now dispatches by role), `PhotoWorkspace.tsx` (`addAdjustmentLayer`/`addTextLayer`/`addShapeLayer`/`setWatermarkAnchor`, watermark file input and anchor-preset buttons, role-specific controls in the Layers panel — effect sliders, text content/color/size, shape kind/color/stroke/filled).
  - Verification: `tsc` clean; 10 new unit tests added to `tests/unit/photo-layers.test.ts` (role-specific factory defaults, legacy role-less normalization, the fontSize-zero-preservation regression, hex-color/shapeKind normalization fallbacks, and three adjustment-layer compositing behaviors — pixel change with no supplied buffer, opacity-gated no-op, visibility-gated no-op) — 24/24 pass; the full Photo-prefixed unit suite (26 files, 260 tests) passes; production build clean; a new e2e case (`photo.spec.ts` — "adjustment, text, shape, and watermark layers can each be added and configured") passes on desktop+mobile Chromium; the full focused Photo browser matrix (141 cases across all 7 Photo spec files) passes with zero regressions — confirmed with a clean serial (`--workers=1`) desktop-chromium run after an initial parallel-worker run showed unrelated startup-contention flakes (different early tests failing each run, all passing when serialized), consistent with dev-server contention under 4 concurrent workers rather than a code regression.
  - Remaining Phase 4 work: Task 4.4 (warp/liquify and deterministic filters — capabilities 102–108). Phases 5–7 (multi-image processing, workflow/export depth, final audit/integration/deployment) remain open.
- [x] Phase 4 / Task 4.4 — mesh warp, liquify, and deterministic detail filters, implemented 2026-09-22, completing Phase 4: credits capabilities 54 (mesh warp), 55 (liquify push/pull/restore), 102 (high-pass detail), 103 (Gaussian blur), 104 (median filter), 105 (edge-preserving/bilateral smoothing), 106 (frequency-separation workflow, its editable half — a boost/soften control on the extracted high-frequency band relative to the low-frequency base), 107 (defringe by hue/range, distinct from the existing global chromatic-aberration channel shift), 108 (moiré reduction), 109 (hot/dead-pixel correction), and 110 (dust/spot visualization).
  - Design — mesh warp/liquify (new `photo-warp.ts`, mirroring `photo-geometry.ts`'s existing inverse-mapping/bilinear-sample pattern): a fixed 5x5 bounded control-point grid (`PhotoMeshWarpPoint[]`, ±0.12 normalized displacement) that a canvas drag nudges via proximity-weighted smoothstep falloff, rather than single-handle dragging — a deliberately coarse, structural distortion tool distinct from liquify's freeform brush. Liquify (`PhotoLiquifyStroke[]`) is push/pull/restore brush strokes accumulating a displacement field: push/pull strokes add bounded displacement (tangent-direction for push, toward-brush-center for pull, both smoothstep-weighted by distance from each path point); restore strokes then shrink whatever displacement earlier strokes already added within their own radius — the same "erase toward zero" idea this codebase already uses for brush erase mode, not independent negative displacement. Both feed one shared `mapPhotoWarpPoint`, applied in `photo-renderer.ts` as a geometry-stage pass right after the existing lens/perspective warp, using the exact same subtract-forward-displacement inverse-mapping approximation `mapPhotoGeometryPoint` already uses for the same reason (exact at zero displacement, coherent across this tool's bounded range).
  - Design — detail filters (new `photo-detail-filters.ts`): nine deterministic, bounded pixel functions, each independently a no-op at zero and each with an explicit performance cap (fixed small radii: bilateral 5x5, median up to 7x7, hot-pixel 3x3) so cost stays interactive rather than scaling with a user-chosen radius. Gaussian blur is a 3-pass box-blur approximation (a standard, well-established technique). Frequency separation exposes only its editable half as a single scalar (`frequencySeparationDetail`, -1 softens toward the low-frequency base, +1 boosts the high-frequency residual) rather than two independently-edited virtual layers, since that boost/soften control is the actual editing operation the workflow performs on the separated bands. Defringe and moiré reduction both gate their effect on local edge/energy so flat regions of a matching hue or repetitive pattern are left untouched rather than uniformly softened. Hot-pixel correction compares each channel against its 8-neighbor median and replaces only when the deviation exceeds a threshold that tightens as the control increases — by design this can also catch a feature's sharp corners (a known characteristic of simple neighborhood-median defect correction, not unique to this implementation), not just literal single-pixel sensor defects.
  - Dust/spot visualization (capability 110) is delivered as a fourth ephemeral canvas overlay mode (`'dust'`, alongside the existing 'clipping'/'focus'/'exposure-zones') in `PhotoCanvas.tsx`/`photo-scopes.ts`, not persisted recipe state — matching exactly how the other three preview-only overlays already work, so it never affects rendered/exported pixels and carries no dead recipe field.
  - Legacy compatibility: recipes saved before this task have no `meshWarp`/`liquifyStrokes`/`detailFilters` fields; normalization defaults them to `null`, `[]`, and explicit neutral defaults respectively, and an all-neutral mesh (every control point at zero displacement) normalizes back to `null` so an edited-then-reset recipe stays byte-minimal. A wrong-size mesh (a hand-edited or corrupted recipe) normalizes to `null` rather than throwing.
  - Files: `photo-types.ts` (`PhotoMeshWarpPoint`, `PhotoLiquifyMode`, `PhotoLiquifyStroke`, `PhotoDefringe`, `PhotoDetailFilters`, new `PhotoRecipe` fields), `photo-warp.ts` (new), `photo-detail-filters.ts` (new), `photo-engine.ts` (normalization/clone/default wiring, `applyDetailFilters` call and `isFullyNeutral` fast-path extension), `photo-renderer.ts` (`warpPhotoMeshLiquifyPixels` geometry-stage call), `photo-interaction.ts` (`applyMeshWarpDrag`, `applyLiquifyStroke`), `photo-scopes.ts` (`'dust'` overlay mode), `PhotoCanvas.tsx` (`'warp'`/`'warp-mesh'`/`'warp-liquify'` interaction kinds, Dust visualization toolbar button), `PhotoWorkspace.tsx` (new "Warp & detail" tab/panel: mesh-warp drag toggle + reset, liquify mode/radius/strength + paint toggle + clear, and sliders for all nine detail filters plus the three defringe controls).
  - Verification: `tsc` clean; 42 new unit tests across two new files — `tests/unit/photo-warp.test.ts` (18: grid neutrality/normalization/clamping, gesture proximity-weighting, push/pull/restore displacement math, full pixel-warp no-op and active-warp behavior, dimension-mismatch rejection) and `tests/unit/photo-detail-filters.test.ts` (24: per-filter zero-is-no-op plus one behavioral case each, normalization/clamping, and the combined-pipeline no-op/active checks) — all pass; the full Photo-prefixed unit suite (28 files, 302 tests) passes; production build clean; 2 new e2e cases (`photo.spec.ts` — "mesh warp, liquify, and deterministic detail filters are reachable and reversible" and "dust visualization renders a preview-only overlay distinct from clipping/focus overlays") pass on desktop+mobile Chromium; the full focused Photo browser matrix (145 cases across all 7 Photo spec files) passes with zero regressions. An initial run against a leftover `vite preview` process from an earlier session (serving a stale pre-Task-4.3 build, confirmed by `netstat`/`Get-Process` and the missing Layers/Warp-&-detail tabs in the failure's page snapshot) produced spurious "button not found" failures; killing that stale process and rebuilding produced a clean run, including with 4 parallel workers — not a code defect.
  - **Phase 4 (Tasks 4.1–4.4) is now fully complete.** Phase 5 (multi-image processing — shared registration/merge worker, exposure fusion/HDR, panorama stitching) is next; Phases 6–7 (workflow/export depth, final audit/integration/deployment) remain open after that.
  - Count correction (recorded 2026-09-23): the Task 4.4 evidence above says "28 files, 302 tests" and "145 cases"; exact recounts show the Photo-prefixed unit suite was **27 files / 302 tests** and the 7-file browser matrix was **146 cases** at `c18506f0` (`playwright test --list`; no browser test changed between then and Task 5.1). The browser figure had been derived as 141 + 4 instead of measured. No test was missing or skipped; these are transcription errors only.
- [x] Phase 5 / Task 5.1 — shared registration/merge worker foundation, implemented 2026-09-23.
  - **No capability credit taken for this task.** It is infrastructure only: nothing in the workspace UI starts a merge yet, and per this tracker's completion contract infrastructure never increments the count. Capabilities 128–137 become creditable only as Tasks 5.2–5.4 make each operation reachable.
  - Dependency decision (checked 2026-09-23): an untracked `.probe-opencv/` scratch directory from an earlier, unrecorded session had started evaluating `@techstark/opencv-js` in a Web Worker but left no findings, and its own worker threw (`DMatchVector.get()` returns plain value objects with no `delete()` in OpenCV.js 5). After fixing that and making the probe verify its known answer, the evidence was: ORB + RANSAC homography alone recovered a known (+8, −5) shift only to ~1.5 px on a periodic grid, while ECC refinement (`findTransformECC`, `MOTION_TRANSLATION`) recovered (8.022, −5.035) with correlation 0.9987. The build also ships OpenCV's photo module — `AlignMTB`, `MergeMertens`, `CalibrateDebevec`/`MergeDebevec`, `TonemapReinhard` — which Tasks 5.2 (capabilities 128–130) and 136 need; it does **not** ship `Stitcher`, SIFT, AKAZE, `phaseCorrelate`, or `estimateAffinePartial2D`, so Task 5.3 panorama must be composed from ORB, `findHomography`, and warps rather than a turnkey stitcher. npm metadata: `5.0.0-release.1` is the `latest` dist-tag (this publisher marks every stable build `-release.N`), Apache-2.0, bundles OpenCV 5.0.0, zero runtime dependencies, and no install-time scripts, so it passes the repository's pnpm supply-chain policy. Pinned exactly; `pnpm audit --prod` reports no known vulnerabilities. Reference: [package](https://www.npmjs.com/package/@techstark/opencv-js), [wrapper source](https://github.com/TechStark/opencv-js), [OpenCV 5.0.0](https://github.com/opencv/opencv/tree/5.0.0).
  - Design (mirrors the existing TIFF/RAW split: pure logic → thin worker → client with queue and deadline): `merge/photo-merge-types.ts` defines the message protocol, limits equal to the TIFF/RAW import ceilings (2–9 sources, 4096 px edge, 16 MP per frame, 768 MiB estimated working memory, 90 s per job), and named diagnostic codes. `merge/photo-merge-plan.ts` validates count, per-frame dimensions, size, matching dimensions (required for stacks/HDR, not panorama), and an operation-specific memory estimate **before** any pixel work, naming the offending photo in every refusal. `merge/photo-registration.ts` is the OpenCV adapter, typed against a narrow local interface of only the calls it uses: an ORB/RANSAC coarse estimate seeds ECC sub-pixel refinement (translation, euclidean, or homography), both on a copy downscaled to at most 1024 px, and the result is mapped back to full resolution through OpenCV's INTER_AREA pixel-center convention (`x_small = s·x + (s−1)/2`) rather than a plain scale, so the scale-back is exact. A feature-poor frame falls back to identity-seeded refinement; a frame that neither matches nor converges fails with a per-photo `registration-failed` diagnostic. Every OpenCV allocation for a request is tracked and released in `finally`, since WASM heap memory is not garbage collected. `warpFrameToReference` resamples a frame onto the reference grid with uncovered pixels left fully transparent, so later merges can tell coverage from padding. `merge/photo-merge-handler.ts` never throws: malformed input, an engine that cannot load, an unalignable frame, or an unexpected engine fault each become a diagnostic for that request id only. `merge/photo-merge.worker.ts` runs requests strictly one at a time. `merge/photo-merge-client.ts` creates its worker lazily, runs one job at a time, transfers source buffers, enforces the deadline, and terminates a timed-out or crashed worker so the next job starts on a fresh one.
  - Engine loading: the worker statically imports the package (`merge/photo-merge-engine.ts`); laziness comes from the worker itself being created only when a merge starts. A dynamic `import()` was tried first and rejected: promise resolution treats the module namespace as a thenable, and this CJS package's interop namespace can expose its default export's `then`, which throws. `merge/photo-cv-runtime.ts` normalises the three shapes the build can export (initialised module, promise of one, or an Emscripten runtime still starting). Vitest's CJS interop hits the same `then` problem even for a static import, so the unit tests load the real engine through Node's native `require` and the same resolver.
  - Bundle and caching: the engine lands only in `assets/photo-merge.worker-*.js` (15.57 MB in the probe build) and is excluded from PWA precache with a bounded CacheFirst runtime rule (`photo-merge-engine`), matching the LibRaw and LittleCMS precedent. Because no UI imports the client yet, this app build emits no merge chunk at all: precache is unchanged from Task 4.4 at 135 entries / 14,387.88 KiB, and `sw.js` carries only the runtime rule. The worker chunk's emission from the app build is first observable when Task 5.2 wires the client. `public/photo-studio/opencv-LICENSE.txt` ships the wrapper and OpenCV Apache-2.0 notice.
  - Files: `package.json`/`pnpm-lock.yaml` (exact pin), `vite.config.ts` (precache exclusion + runtime cache rule), `public/photo-studio/opencv-LICENSE.txt`, and new `src/tools/photo/merge/{photo-merge-types,photo-merge-plan,photo-registration,photo-cv-runtime,photo-merge-engine,photo-merge-handler,photo-merge.worker,photo-merge-client}.ts`.
  - Verification: `tsc` clean; 26 new unit tests in `tests/unit/photo-merge.test.ts` — bounded-queue planning (count/size/mismatch/memory refusals naming the photo, panorama allowed to mix sizes), real-engine registration (a known translation recovered within 0.05 px; a 2048×1536 frame recovered within 0.35 px through the downscale-and-map-back path; a 1.5° rotation plus translation recovered within 0.3 px at the corners with the euclidean model; identical inputs give identical matrices; warping through the registration reproduces the reference with mean error under 3 levels and transparent uncovered pixels; a featureless frame fails as `registration-failed` for photo 2; an `align` request returns one reference-grid raster per source), engine runtime resolution, and error isolation (malformed messages, an unloadable engine, and a mid-request engine fault each fail only their own request, and the next request succeeds; the allocation scope frees every handle even when one release throws); plus client behavior with a fake worker (lazy creation, buffer transfer, stale-id replies ignored, one job at a time, worker diagnostics passed through, deadline terminates the worker and the next job gets a fresh one, crash recovery, no-Worker environments, dispose). Fixtures crop both frames from one larger scene so a shift reveals real content, not an artificial black border — an early fixture that padded with black biased unmasked ECC by ~0.23 px, which is a fixture artifact, not something real camera frames produce. Full Photo-prefixed unit suite: 28 files / 328 tests pass (29 / 330 with `e2e-spec-selection`). Production build clean. The full browser path was proven in headless Chromium through the scratch probe using the real `createPhotoMergeClient`: it recovered a known (+7, −3) shift as (6.9999, −3.0006), correlation 0.99995, in 1.5 s including engine load, with the calling entry chunk at 3.8 KB. Full 7-file Photo browser matrix: see the commit-time evidence line in Current state.
  - Environment note: two orphaned `vite preview` processes were found and stopped during this pass — one from this task's own probe (port 4189) and one left on port 4173 since the Task 4.4 browser runs, serving a cached copy of a build that no longer existed on disk. The 4173 orphan is the same failure mode as the Task 4.4 false alarm: Playwright's `reuseExistingServer` silently tests whatever answers on that port. Confirm the port is free before any browser run.

- [x] Phase 5 / Tasks 5.2–5.4 — exposure fusion, HDR merge and tone mapping, panorama stitching, and focus/average/median stacks, implemented 2026-09-24. Credits capabilities **128** (exposure fusion), **129** (HDR merge), **130** (tone mapping), **131** (panorama), **132** (projection and crop controls), **133** (focus stack), **134** (average stack), **135** (median stack), **136** (alignment before merge), and **137** (merge diagnostics).
  - Evidence for the engine surface: the pinned `@techstark/opencv-js@5.0.0-release.1` build was probed directly and exposes `MergeMertens` (two-argument overload bound as `process1`), `CalibrateDebevec`, `MergeDebevec`, `TonemapReinhard`, `TonemapDrago`, and `TonemapMantiuk`. Parameter meanings and ranges follow the official OpenCV 5.0.0 `modules/photo/include/opencv2/photo.hpp` declarations (Mertens output converts to 8-bit by ×255; tonemappers output `CV_32FC3` in [0, 1]; Debevec takes per-image exposure times; Reinhard intensity −8…8, adaptations 0…1; Drago bias 0…1; Mantiuk scale). No dependency was added: shutter speed and 35 mm focal length are read with the already-pinned `exifreader@4.44.0` (lazy `import()`), and results are encoded with the browser's own PNG encoder.
  - Design: pure, deterministic pixel operations live in `merge/photo-merge-ops.ts` (average/median/focus stacks that ignore uncovered alpha-0 pixels; cylindrical projection; a feather-blended panorama compositor with chained neighbour gain compensation and uniform downscaling to the 8,192 px / 16 MP output ceiling; largest fully-covered rectangle crop; Horn's closed-form rotation fit). `merge/photo-merge-exposure.ts` wraps the OpenCV HDR calls. `merge/photo-merge-run.ts` orchestrates one job; `photo-merge-handler.ts` gained a validated `merge` request whose options are rebuilt field by field from untrusted input; the client gained `merge()` and a user `cancel()` that terminates the running worker.
  - Panorama registration is features-only (ORB + cross-checked matching + RANSAC, masked away from transparent projection borders) because ECC assumes near-complete overlap. The final model is fitted to RANSAC inliers with few parameters so it stays well-conditioned beyond a narrow overlap band: cylindrical frames use the median inlier translation; flat (planar) frames use a 3-parameter camera rotation `K·R·K⁻¹` from the lens field of view. A free 8-parameter homography was tried first and rejected with evidence: on a 42 %-overlap fixture it produced a 0.83 scale error in the third frame.
  - UI (`PhotoMergePanel.tsx`, new **Merge** tab): merge type with plain-language guidance, add/reorder/remove sources (TIFF/RAW decode through the shared raster path), reference-photo choice, per-photo shutter speed for HDR (prefilled from EXIF, accepts `1/250`, `0.5`, `2s`), alignment choice including tripod/no alignment, operation-specific controls, crop-to-coverage, a pre-run plan (source count, dimensions, memory estimate vs. limit, exposure problems) that disables the run when invalid, **Check alignment** with per-photo confidence, cancel, and result notes. The result opens as an ordinary PNG source; the workspace shows the merge summary after the new source's first preview instead of letting "Preview updated" overwrite it (a race found by the browser test).
  - Verification: `tests/unit/photo-merge-ops.test.ts` (17), `tests/unit/photo-merge-run.test.ts` (19, real engine: fusion reduces clipping versus the brightest frame and crushing versus the darkest; HDR keeps scene order for all three operators; noisy aligned average/median stacks are closer to the clean scene than one frame; crop-to-coverage; planar stitching recovers a 20° yaw within a pixel and matches the analytically true composite within 2.5 levels mean error; cylindrical steps equal f·θ; non-overlapping frames fail naming the pair; validation). `tests/e2e/photo-merge.spec.ts` (5 cases × 2 projects) covers an average stack opening as an editable photo, HDR gating on shutter speeds, size-mismatch diagnostics, handheld exposure fusion through the real worker with alignment confidence, and 320 CSS px reflow. The selector maps Photo changes to the new spec.

- [x] Phase 6 / Tasks 6.1–6.7 — workflow and export depth, implemented 2026-09-24. Credits capabilities **140–152** and **154–164**, plus scattered capabilities **20, 22, 23, 24, 25, and 127** closed in the same pass.
  - 6.1 (140, 141, 142): `photo-recipe-groups.ts` maps every recipe field to one of 18 groups through a `Record` keyed by the recipe's own fields (adding a field without a group fails compilation; a unit test also checks coverage). Inspect & workflow → **Copy & paste settings** copies/pastes chosen groups as one undo step; the command-bar Copy/Paste keep whole-recipe behaviour. Batch export's **Edits to apply** can sync only chosen groups (default "look" groups, skipping crop, warp, masks, retouch, layers) onto each queued photo.
  - 6.2 (143, 152, 162, 163): `photo-export-queue.ts` runs a reorderable queue one item at a time with per-item cancel (a cancel during rendering discards that output unsaved), retry, and failure isolation; finished blobs go straight to the output sink. File-name rules support `{name}`, `{n}`/`{n:3}`, `{date}`, `{preset}`, `{w}`, `{h}` with a live preview; duplicate names become "name (2).ext" case-insensitively, and folder output never overwrites existing files. A JSON or CSV summary (CSV neutralises spreadsheet formulas) is available after batch and multi-version runs.
  - 6.3 (144, 148, 159): `photo-templates.ts` stores export presets, metadata templates, and watermark presets in a separate IndexedDB database (`inmotools.photo-studio.templates`), so the project database was not migrated; a session-only store is used and disclosed when IndexedDB is refused. Metadata templates drop location fields unless explicitly opted in and fill only empty fields at export. Watermark presets size themselves as a fraction of the output's shorter side and inset by their real size (the renderer's fixed text padding is shared through `TEXT_LAYER_PADDING`); they are stamped on export copies only and can also be added as an ordinary layer from the Layers panel. Templates import/export as versioned JSON files.
  - 6.4 (145, 146, 149, 160, 161): named export presets capture format, quality, AVIF lossless, size, don't-enlarge, resampling, sharpening, background, metadata policy and template, watermark, and naming rule; batch uses the active settings; **Export several versions at once** renders one photo through several presets. Delivery sizes are described by purpose (4K screen, web, social feed, thumbnail), not brands. A **Don't enlarge** option was added after browser testing showed delivery sizes would upscale small photos.
  - 6.5 (154, 155, 156, 157, 158): `photo-resample.ts` provides Lanczos-3, Mitchell, Catmull-Rom, and bilinear kernels in linear light with premultiplied alpha, applied by the export renderer at the geometry stage (fallback to the browser scaler, reported, when the unscaled frame exceeds canvas limits). `photo-tiff-writer.ts` writes baseline 8-bit RGB/RGBA TIFF (tags per libtiff `tiff.h`; ICC 34675; XMP 700), verified with the independent `tiff` decoder. **16-bit TIFF is intentionally not offered:** Photo Studio edits in 8 bits, so 16-bit samples would be padded values that misrepresent precision. AVIF export uses the exact-pinned `@jsquash/avif@2.1.1` (Apache-2.0; bundles libavif v1.0.1, libaom v3.7.0, libsharpyuv; latest npm dist-tag published 2025-05-20; no install scripts; transitive `wasm-feature-detect@1.9.0`), importing its single-threaded glue so behaviour does not depend on cross-origin isolation; it runs in a lazily created worker with a 3-minute deadline and is excluded from precache. Canvas AVIF encoding was not used because MDN browser-compat-data lists only JPEG, PNG, and WebP for `toBlob`. AVIF cannot carry embedded XMP or ICC here: XMP falls back to the sidecar with a clear message, and AVIF is disabled (with the reason shown) when an ICC output profile is set. Each format states lossless/lossy behaviour. Notices ship in `public/photo-studio/avif-LICENSE.txt` (upstream license texts at the pinned versions).
  - 6.6 (147, 150): `photo-print.ts` uses pdf-lib's `PageSizes` for ISO/North American papers plus 4×6, 5×7, 8×10 in photo sizes; the **Print size planner** reports fitted size, density, and plain-language guidance, and can size the export for a paper at a target ppi. **Contact sheet** renders the queued photos (with or without current edits) into a multi-page PDF (pdf-lib, lazily loaded) or PNG pages. No printer-driver control is claimed.
  - 6.7 (164): File System Access is used only as progressive enhancement per the WICG specification (pickers invoked directly from clicks; `AbortError` treated as cancel): **Save to file…** for single exports, **Export to a folder…** for batch/multi-version, and the Open photo picker remembers the last folder via its `id`. Ordinary downloads and file input remain the fallback everywhere.
  - 151, 20, 22–25, 127: **Compare edits** lists field-level differences between current edits, snapshots, and saved projects/variants. Snapshots can be renamed, duplicated, and deleted. User presets can be renamed and duplicated (delete/import/export already existed). Deleting a local project keeps an in-memory copy (record plus source bytes) and offers **Undo delete**. Export renders composite layers into a copy without mutating project state (127), demonstrated by export-only watermark stamping with the project's layer list unchanged.
  - Known boundary: when a browser's real canvas limit forces a safety-scaled render, a watermark planned for the requested size is proportionally larger on that smaller render. The UI already warns before any known-oversized single export.
  - Verification: new unit files `photo-resample` (12), `photo-tiff-writer` (6), `photo-recipe-groups` (6), `photo-templates` (14, including export settings and AVIF boundaries), `photo-export-queue` (7), `photo-print` (7), plus a don't-enlarge case in `photo-export-dimensions`. New browser spec `photo-workflow.spec.ts` (14 cases × desktop/mobile) covers TIFF + Lanczos output read back, AVIF encode/decode and sidecar fallback, preset persistence across reload, multi-version export, batch naming/failure/retry/CSV summary, look-only batch sync, metadata templates, watermark presets (export-only, then as a layer), print planner, contact-sheet PDF, partial copy/paste, snapshot rename/duplicate/compare/delete, undo delete, and 320 CSS px reflow with every new section open. Existing specs were kept passing by giving new controls distinct accessible names.

- [x] Scattered capabilities closed 2026-09-25: **8, 9, 10, 12, 29, 50, 52, 53, 56, 57, 61, 64, 111, 123** (this also closes Phase 1 / Task 1.5).
  - 8 (RAW exposure): `codecs/raw-decoder.ts` writes LibRaw's `exp_correc` / `exp_shift` / `exp_preser` (the pre-demosaic exposure correction documented in LibRaw's API notes; shift is linear 0.25–8, i.e. −2…+3 EV) into the decoder's parameter struct. The binding exposes no setter for these fields, so their offsets are located at runtime from known neighbouring fields that do have setters; decoding refuses the exposure change (instead of guessing) when that layout check fails. **RAW highlight protection** (`exp_preser`) appears only for positive EV. Normalized, durable, undoable like the other RAW settings.
  - 9, 10: Inspect & workflow → **Original file details** reads EXIF/GPS/ICC with the already-shipped lazy `exifreader` and lists camera, capture, location, and the embedded colour profile (description, colour space, class, version), separate from the export metadata policy.
  - 12: a SHA-256 of the source bytes (`crypto.subtle`) is stored on each project; importing a file already saved as another local project offers **Open saved project** or **Keep this copy**.
  - 29: **Hold for original** button and holding the `\` key show the unedited photo; releasing returns to the edit.
  - 111: Detail & noise and Deterministic detail filters warn when the preview is below 100 % and offer **Check at 100%**, a draggable/keyboard-pannable view rendered by the export pipeline at the frame's real size.
  - 52, 53, 56, 57 (`photo-transform.ts`, `PhotoTransformControls.tsx`, Crop & geometry): **Free transform** (move, independent width/height scale with optional proportion lock, rotation) and **Corner perspective** (four pinned corners, homography) are inverse-mapped with bilinear sampling after lens/keystone and before mesh warp/liquify, in the shared renderer. **Canvas size** adds transparent or solid-colour space per side after all edits (layers, masks, and retouch stay on the photo; on-canvas input and overlays are mapped through the inset; JPEG flattens transparent canvas onto the export background). **Trim transparent edges** renders at full size, finds fully transparent borders, and stores the trim as negative canvas sides so it scales with any export size. Watermarks are sized against the photo inside the canvas.
  - 61: **Selective color** (Edit panel) adjusts cyan/magenta/yellow/black per family (reds…blacks) in relative or absolute mode; a deterministic model documented in `photo-selective-color.ts`, applied after HSL ranges.
  - 64: **Pick neutral point** (White balance) renders only the stages before white balance, averages a 5 × 5 patch in linear light, and solves temperature/tint against the renderer's own gains (`whiteBalanceMultipliers`, shared by both). Targets outside the controls' range are set as close as possible and say so.
  - 123: **Layer groups** (name, visibility, opacity that multiplies members', ungroup) in the Layers panel; groups are part of the Layers copy group.
  - 50: **Rulers & guides** (canvas toolbar) shows rulers labelled in output pixels and up to 12 numeric guides; view-only, never saved or exported.
  - QoL found by the mobile browser run: arming any canvas tool now scrolls the photo back into view when the stacked controls had pushed it off-screen.
  - Verification: unit files `photo-transform` (10), `photo-selective-color` (4), `photo-source-metadata` (6), `photo-white-balance-picker` (3), extended `photo-raw`; browser spec `photo-editing-extras.spec.ts` covers canvas size/colour/trim with exported pixels, corner perspective, selective color isolation, eyedropper neutrality, group hide/ungroup, rulers/guides, hold-original (key and pointer), the 100 % view's natural size, source details and the duplicate prompt, and RAW exposure brightening.

## Remaining release work

1. Phase 1 / Task 1.5 is complete (RAW exposure baseline, see above). If the LibRaw binding is upgraded, rerun `tests/unit/photo-raw.test.ts`: the exposure-field layout check must still pass, and decoding refuses the exposure change rather than writing to unverified offsets if it does not.
2. Preserve the documented TIFF support boundary below; any additional variant must gain bounded decode and fixture evidence before being advertised as supported.
3. All ledger capabilities are implemented. Next is Phase 7 (audit, integration with `main`, Pages proof, tracker closure). Inspect & workflow → **Open-source components** links every shipped notice under `public/photo-studio/` (done 2026-09-25; a browser test fetches each link). Reverify official documentation and npm stable metadata before any dependency change, use exact pins only, and maintain the 164-item capability ledger without silent omissions.
4. Done 2026-09-25: `origin/main` (`c290290e`, 317 commits ahead) merged into `claude/photo-tool-completion-ama1g1` with a merge commit once `cdn.sheetjs.com` was reachable. Conflicts resolved as planned: `.tasks/IN_PROGRESS.md` keeps both sides minus main's stale "Current milestone: A" Photo line; `package.json` is main's list plus the five Photo pins (`@colorhythm/libraw-wasm`, `@jsquash/avif`, `@techstark/opencv-js`, `lcms-wasm`, `tiff`); `pnpm-workspace.yaml` unions `allowBuilds` and `overrides`; `pnpm-lock.yaml` was regenerated with `pnpm install --lockfile-only` (pnpm 12.3.4) and its diff against `main` is additions only.
5. Done in the same merge commit: the Photo entry in `src/catalog.ts` now lists TIFF and camera RAW input, two-to-nine-photo merges, and TIFF/AVIF output plus PDF/PNG contact sheets.
6. Review the exact changed-file set, run exact merge-state validation, verify the Pages deployment, and close shared task records only after every completion gate is evidenced.

## Ledger correction — 2026-09-21

Before starting Task 3.3, this file's own "Remaining release work" claimed Task 3.2 mask management (capabilities 86, 87, 90, 91, 98) was still open. A direct read of `PhotoWorkspace.tsx`, `photo-mask.ts`, and `tests/e2e/photo.spec.ts` found it was already fully implemented and already covered by a dedicated passing e2e test (`local masks rename, duplicate, bypass, visualize, and compose with an active selection`, confirmed green on desktop+mobile before any new work began) and a dedicated unit suite (`tests/unit/photo-mask-management.test.ts`). This tracker had simply not been updated after that work landed. Recorded here so the capability count below reflects actual, verified code state rather than the stale planning note — do not repeat capabilities 86/87/90/91/98 as future work.

## Expanded capability ledger checkpoint

- Checkpoint 2026-09-25: **164 of 164 capabilities verified.** This pass closed 8, 9, 10, 12, 29, 50, 52, 53, 56, 57, 61, 64, 111, and 123. Phase 7 must still revalidate full-wiring claims before release.
- Checkpoint 2026-09-24 (Phase 6): **150 of 164 capabilities verified; 14 remain incomplete.** Phase 6 adds 140–152 and 154–164; the same pass closes 20, 22, 23, 24, 25, and 127. Remaining IDs: 8 (partial), 9, 10, 12, 29, 50, 52, 53, 56, 57, 61, 64, 111, 123.
- Checkpoint 2026-09-24 (Phase 5): 120 of 164 verified; Tasks 5.2–5.4 added 128–137.
- Previous checkpoint: **110 of 164 capabilities verified; 54 remain incomplete**, including partially implemented items. That pass added capabilities 54, 55, 102, 103, 104, 105, 106, 107, 108, 109, 110 (Task 4.4 — mesh warp, liquify, and the nine deterministic detail filters) to the prior 99/164 checkpoint. **Phase 4 (Tasks 4.1–4.4) is now fully credited and complete.** Phase 3 (Tasks 3.1–3.4) remains fully credited. Capability 123 (layer grouping) remains explicitly conditional in the plan ("if the implemented stack complexity justifies it") and is deferred; nothing in the current layer stack yet creates the depth that would need it. Capabilities 61 (selective color) and 64 (neutral-target eyedropper) remain open; no adjacency credit is taken. Capability 8 remains partial because RAW exposure baseline is not implemented. Capability 93 (brush hardness) is credited via the existing shared `feather` control, which is the same concept under a different name in this codebase, not a separate dedicated control. Ordinary post-raster adjustments are not credited as sensor-level development.
- Full-wiring IDs: **all 1–164** in `docs/superpowers/specs/2026-09-12-photo-studio-capability-expansion.md`.
- The expanded ledger is separate from the original base inventory and test totals. Phase 7 must still account for every remaining ID and revalidate full-wiring claims before release.

## Selection milestone and support boundary

- Implemented at `8934f1c9771f087a4008787908e27bdc319a0464` without a new dependency or a parallel render path. `src/tools/photo/photo-selection.ts` is the pure source for normalization, deep cloning, geometry/color/luminance weighting, combination, and local-mask conversion. Version-1 recipes with no selection remain valid and normalize to `null`; history, projects, presets, and copy/paste deep-clone selection polygons and converted selection masks.
- An active selection holds at most 64 operations and 5,000 normalized points per polygon. Replace discards superseded operations; add uses union/max, subtract removes incoming weight, and intersect retains shared weight. Rectangle, ellipse, and polygon boundaries use normalized signed-distance weighting; color tolerance uses normalized RGB distance; luminance uses display-referred Rec. 709 coefficients. Feather is capped at 25% of normalized image space and growth/shrinkage at ±25%. Degenerate polygons and empty selections do not commit no-op history entries.
- Rectangle, ellipse, and lasso are available through direct image-space canvas gestures. Keyboard users can create centered rectangle, ellipse, and polygon selections and operate all combine/refinement/clear/conversion controls through labeled DOM controls. Rendered-color sampling reads the current preview pixel. The live cyan overlay is view-only, capped to 1,024 pixels on its long edge, and does not change exports.
- Conversion is one reversible history step: the active selection clears and an enabled local adjustment receives the normalized composed selection mask with a visible default +0.5 EV effect. The shared local-adjustment engine evaluates that mask for preview, single export, and batch export; color/luminance predicates remain live against the working pixel values rather than becoming a frozen bitmap. Mask rename/duplicate, overlay appearance controls, and mask-level composition remain Task 3.2 and are not claimed here.
- Focused RED evidence first exposed replace retaining superseded serialized operations even though its effective weight was correct. The append contract now discards the old stack on replace, with unit regression coverage. Final evidence is 169/169 non-RAW Photo unit/selector tests, 45/45 maintained tracked RAW contracts, a successful production/PWA build, and 132/132 browser cases across desktop/mobile with one worker.

## Inspection scopes milestone and support boundary

- Implemented at `408b4f07525ec43fe11f3ae83da8f23a32276dc5` without a new dependency or a second render pipeline. `src/tools/photo/photo-scopes.ts` is the pure deterministic analysis/overlay source; `PhotoScopes.tsx` decodes only the current rendered preview while the panel is open, caps its analysis raster at 640 pixels on the long edge, and samples at most 200,000 positions.
- The inspection panel provides a 256×128 luminance waveform, independent 256×128 red/green/blue parade channels, a 128×128 YCbCr vectorscope, and an eleven-zone distribution measured in stops around 18% middle gray. The focus/detail overlay uses bounded horizontal/vertical luminance energy; the exposure overlay uses the same zone classifier as the distribution. These are display-referred inspection aids, do not mutate the recipe or export, and do not claim camera-native or scene-linear measurements.
- The sampler retains at most eight normalized image-space pins, reports current intrinsic preview pixel coordinates, refreshes values after edits, and clears them on source replacement. RGB/HSL/hex remain available normally; XYZ and Lab are reported against the D65 sRGB display encoding only while an assigned or active proof transform supplies the color-management context. They are not spectrophotometer readings, absolute scene color, or monitor-calibration claims. Proof before/after values continue to use the real LittleCMS proof path.
- A mutually exclusive clipping/focus/exposure overlay control prevents stacked diagnostic ambiguity. Capability 39 is satisfied by the Task 2.5 LittleCMS proof-profile gamut output, not by recoloring unrelated scope data. The minimap appears above 100% zoom and maps clicks into the canvas scroll extent; checkerboard, dark gray, light gray, and black backgrounds are view-only preferences. Desktop/mobile pointer behavior, edit-stable pins, profile-aware Lab/XYZ, overlay switching, navigator visibility, responsive reflow, and the complete prior Photo surface are covered by the 130-case browser gate.

## 3D Cube LUT milestone and support boundary

- Runtime source of truth is `src/tools/photo/photo-lut.ts`; recipe ownership is `PhotoRecipe.lut`, with missing legacy version-1 fields normalized to `null`. The encoded Float32 grid is immutable across history clones; decoded grids use a four-entry module-local cache and are prepared once per render rather than decoded or normalized per pixel.
- Supported input is a text 3D Cube with `LUT_3D_SIZE` 2–65, exact `size³` RGB rows in standard red-fastest order, optional quoted/unquoted `TITLE`, `DOMAIN_MIN`/`DOMAIN_MAX`, or the common scalar `LUT_3D_INPUT_RANGE`. Files are capped at 16 MiB; every number must be finite and each domain minimum must be smaller than its corresponding maximum. UTF-8 BOM, blank lines, and comments outside quoted titles are accepted. Missing/duplicate size, malformed rows, excess/short data, invalid domains, 1D LUTs, and combined 1D/3D files fail explicitly without replacing the active recipe.
- Rendering performs trilinear interpolation in normalized sRGB recipe space after levels, master/per-channel curves, and channel mixing, before vignette/grain and subsequent spatial/local/retouch stages. Strength blends the pre-LUT and LUT result from 0–1 and final display values clamp to 0–1. Preview, single export, and batch export all call this same engine; there is no GPU-only or preview-only path.
- `Export active LUT` serializes the supported 3D grid with current strength mathematically baked into every grid vertex and retains the declared input domain. Re-import round trips the represented transform within Float32/text precision. It intentionally does not claim to encode crop, local edits, retouch, spatial filters, or unrelated global adjustments; the UI states this boundary.
- Dependency decision (checked 2026-09-16): official Three.js `LUTCubeLoader` documentation/source and npm metadata were reverified. npm stable was `three@0.186.0`, while this repository remains exactly pinned to `three@0.185.1`; the addon produces a GPU `Data3DTexture`, does not provide the CPU worker transform required by Photo preview/export, and its permissive loader is not the bounded untrusted-input contract needed here. Upgrading the shared Three dependency would also touch the separately owned CAD/3D surface. Task 2.4 therefore added no dependency, package/lockfile, license, CAD, decoder, or shared-CI change. References: [official LUTCubeLoader docs](https://threejs.org/docs/pages/LUTCubeLoader.html), [official source](https://github.com/mrdoob/three.js/blob/master/examples/jsm/loaders/LUTCubeLoader.js), [npm package](https://www.npmjs.com/package/three), and [Adobe Cube LUT specification archive](https://web.archive.org/web/20220220033515/https://wwwimages2.adobe.com/content/dam/acom/en/products/speedgrade/cc/pdfs/cube-lut-specification-1.0.pdf).

## ICC color-management milestone and support boundary

- Implemented at `24ecb4b2162d11cae8217dccc0e82ace73a429c7` with exact `lcms-wasm@1.0.5`. The wrapper is dynamically imported and its 315.91 kB emitted WASM asset is excluded from universal precaching; a bounded CacheFirst runtime route supports later offline reuse. `public/photo-studio/lcms-LICENSE.txt` ships the wrapper and LittleCMS license/source notices.
- Profiles are capped at 2 MiB and must have a consistent ICC header length/signature and an RGB, CMYK, or gray color-space declaration. Source assignment and output conversion accept RGB profiles; proofing also accepts CMYK and gray. Version-1 recipes remain backward-compatible, store bounded profile bytes plus SHA-256 identity, clone them across history/persistence, and use a four-profile decode cache. Original source bytes are not rewritten.
- The shared preview/export pipeline applies the assigned source transform before ordinary Photo edits. Soft proof and proof-gamut visualization are preview-only; export instead converts edited pixels into the selected RGB output profile and must embed that exact profile. JPEG uses chunked APP2 `ICC_PROFILE`, PNG uses compressed `iCCP`, and WebP uses `ICCP` with the VP8X feature bit. If required profile embedding fails, export fails rather than emitting converted but incorrectly untagged pixels. JPEG transparency is composited onto the selected background in working RGB before output conversion.
- Supported controls are LittleCMS perceptual, relative-colorimetric, saturation, and absolute-colorimetric intents plus black-point compensation. Gamut warning is tied to an active proof profile and uses LittleCMS proof/gamut checking; the UI reports the affected preview-pixel count. The sampler exposes both pre-proof and post-proof RGB/hex values while proofing. This does not claim browser/OS display calibration, monitor profiling, HDR color management, device-link profiles, or preservation of source bit depth beyond the existing 8-bit editing boundary.
- Dependency decision (checked 2026-09-17): official LittleCMS material, the maintained wrapper repository, and npm metadata were reverified before installation. npm stable is `lcms-wasm@1.0.5`; it exposes proofing transforms, gamut checking, intents, and black-point compensation needed by this task. The package bundles LittleCMS 2.16 even though upstream LittleCMS 2.19 is current, so this runtime boundary is explicit rather than presented as latest upstream core. `@kittl/little-cms@1.0.3` was rejected because its public wrapper exposes ordinary transforms but not the required proof/gamut surface. The upstream wrapper incorrectly declares `tape` as a production dependency; the lock resolves exact stable `tape@5.10.2`, root manifests contain no caret ranges, and `pnpm audit --prod` reports no known vulnerabilities. References: [LittleCMS](https://littlecms.com/), [LittleCMS 2.19 release](https://www.littlecms.com/tags/lcms2-2.19/), [official LittleCMS tutorial](https://www.littlecms.com/LittleCMS2.18%20tutorial.pdf), [`lcms-wasm` repository](https://github.com/mattdesl/lcms-wasm), [`lcms-wasm` on npm](https://www.npmjs.com/package/lcms-wasm), and [`@kittl/little-cms` repository](https://github.com/Kittl/Little-CMS).

## TIFF adapter support boundary

- Supported: classic little-/big-endian TIFF, first page only, uniform unsigned 8-/16-bit samples, grayscale/RGB, optional unassociated alpha except WhiteIsZero-plus-alpha, interleaved strips, uncompressed/Deflate compression (8/32946), and no/horizontal prediction. Top-left orientation and standard fill order are required.
- Safety limits: 64 MiB input, 4096-pixel edge / 16-megapixel raster ceiling, exact strip output-size validation, one decoder worker at a time, 20-second decode deadline, sanitized raster-only directory input to the dependency, cache reuse for preview/export, and per-item raster-cache release during batch export.
- Precision/color boundary: original TIFF bytes are retained in local project storage, 16-bit samples remain exact through decode, then the existing editing/export pipeline uses an explicitly disclosed 8-bit PNG raster. ICC transforms are not applied; source color-profile and color-management capabilities remain scheduled rather than being claimed by TIFF import.
- Browser prerequisites: Worker and OffscreenCanvas; Deflate additionally requires DecompressionStream. Missing capabilities and unsupported data preserve the current document and provide conversion guidance.
- Explicitly deferred decoder enhancements: BigTIFF, floating-point samples, further pages, non-top-left orientations, tiled/separate-plane samples, associated alpha, WhiteIsZero-plus-alpha, and LZW/PackBits/JPEG/CCITT compression. The selected dependency's unrestricted decompression paths are not enabled for untrusted input. These boundaries do not represent implemented variants or silently excluded capabilities; future expansion requires bounded adapters and representative fixtures.

## RAW acquisition milestone and support boundary

- Implemented at `5d43c20e811145d2c2b05ba7fc79ac3d62b890f6` with exact `@colorhythm/libraw-wasm@1.1.1` and a package-scoped exact `typed-cstruct@0.11.0` override. LibRaw reports 0.22.1 and 1,274 camera entries; that backend list is not independent Photo fixture evidence for every proprietary camera/variant.
- Acquisition recognizes `.dng`, `.cr2`, `.cr3`, `.nef`, `.arw`, `.raf`, `.orf`, `.rw2`, `.pef`, and `.srw` with empty/generic binary MIME, plus supported RAW MIME names. Explicit text is excluded; misleading extensions do not override native JPEG/PNG MIME. Bounded first-directory DNGVersion scanning and the CR2 marker route TIFF-family RAW before generic TIFF without recursively following metadata pointers.
- Actual pixel/browser evidence uses an original uncompressed 32x32 Bayer DNG. Other extension cases verify selection/routing, not proprietary pixel decoding. Unsupported cameras, missing optional codecs, damaged data, and unsupported output layouts fail with conversion guidance while preserving the current document.
- Bounds: 64 MiB source; 4096 edge / 16 megapixels for stored/active/output geometry; pre-unpack pixel-aspect expansion checks; one shared RAW/TIFF decoder queue; 30-second RAW worker deadline. Workers terminate on success, failure, or timeout; rejected cached tasks can be retried and batch intermediates are released per item. These adapter bounds are not a configurable hard WASM heap ceiling; the selected wrapper does not expose LibRaw's raw-memory-limit setter.
- Processing explicitly selects 16-bit RGB, sRGB primaries/transfer parameters, default camera white balance when available, and disables automatic brightness normalization. Supported three-color RGB sources additionally expose camera/daylight/custom white balance (red/blue 0.25–4 multipliers, both greens fixed at 1), clip/unclip/blend highlights, and Bayer-only AHD/bilinear/VNG/PPG demosaic choices. Foveon, non-RGB, reserved CFA, X-Trans, and linear layouts do not receive unsupported Bayer overrides; descriptive-fact gate tests are not proprietary-camera fixture evidence. Camera-WB absence is disclosed as decoder fallback. Original RAW bytes remain immutable in storage; the owned 16-bit developed intermediate becomes an explicitly disclosed 8-bit raster for the existing editing/export path. Custom source ICC/profile transforms are not claimed.
- RAW settings are an additive optional field on version-1 recipes, defaulted and bounded during normalization; snapshots are independently cloned. Codec cache keys use normalized settings and retain only the latest variant per source; an older rejection cannot evict a newer queued variant. Import/recovery retain a default-developed before raster while the current recipe develops preview and single/batch exports. Numeric multipliers commit on blur/Enter as one edit and have individual neutral resets. Camera make/model, source layout, stored/active geometry, and as-shot WB availability are read-only inspection facts, not automatically serialized export metadata.
- Embedded-preview boundary: import/recovery optionally extracts the existing LibRaw thumbnail before sensor unpack in the same decoder worker. Metadata and extracted payloads are bounded to 8 MiB and 4096 edge / 16 megapixels; JPEG SOF geometry is validated before native decoding. JPEG and unsigned RGB8 thumbnails become at most 1024-edge PNG previews, with native JPEG orientation or LibRaw RGB flip bits. Absent, damaged, oversized, or unsupported thumbnails fall back to full development; consumer errors do not abort RAW decoding. Progress never completes the queue task, extends its deadline, or becomes an editable/export source. The previous photo and edits remain intact until full success. Superseded operations, completion, failure, and unmount clear/revoke interim URLs; resolved cache entries release progress subscribers. Original DNG fixtures verify real RGB/JPEG extraction and orientation, not every proprietary camera's preview layout or a camera-performance benchmark.
- Browser prerequisites: Worker, OffscreenCanvas, WebAssembly; embedded JPEG rasterization additionally uses createImageBitmap. Production emits an 80.74 kB RAW worker and unchanged 853.08 kB WASM asset. Both are on-demand, excluded from universal precaching, and use bounded CacheFirst runtime caching. Offline recovery is verified after loading under an active service worker; first-time offline decoding still needs cached decoder assets. No dependency, lockfile, license, or shared CI changes accompany RAW development controls or embedded previews. Preview extraction reuses the exact installed `@colorhythm/libraw-wasm@1.1.1`; [official wrapper](https://github.com/colorhythm/libraw-wasm), [LibRaw API](https://www.libraw.org/docs/API-CXX.html), and [npm package](https://www.npmjs.com/package/%40colorhythm/libraw-wasm) were checked as of 2026-09-15, with live npm metadata confirming stable 1.1.1.
- Redistribution notices, LibRaw copyright/CDDL/LGPL alternatives, MIT wrapper/runtime licenses, third-party notices, and immutable source/build locations ship in `public/photo-studio/raw-LICENSE.txt`. The npm WASM binary is not modified.
- Closed 2026-09-25: RAW exposure baseline (capability 8) via LibRaw's pre-demosaic `exp_correc`/`exp_shift`/`exp_preser`, written through a runtime-verified parameter layout because the wrapper has no setter; raster exposure remains separate. Still open: broader representative legal proprietary RAW fixtures.

## Risk-based verification cadence

Effective 2026-09-15, development feedback is batched by risk; the feature inventory and final completion gates above are unchanged.

- Use one primary implementer by default. Delegate only a substantial independent task with clear file ownership and an expected payoff greater than coordination/review cost; do not create parallel implementer/reviewer loops for small changes.
- For new behavior or bug fixes, add a meaningful failing contract test before implementation. Related contracts may share one RED run; every changed behavior still needs appropriate coverage, not an arbitrary test quota per capability.
- For at most two related low-risk edits within an already-tested contract, run the affected checks after the pair, or sooner at the subsystem boundary. A single remaining edit must be checked before handoff. This defers a repeated run, not coverage of either edit.
- Check high-risk changes immediately: codecs/untrusted imports, source retention/deletion, persistence/migrations, history isolation, async revision/cancellation, shared preview/export rendering, output limits, or metadata packaging. Uncertain impact, cross-module effects, failures, or flaky results escalate to broader checks; never sample past ambiguity.
- Run development check commands sequentially, without overlapping build/unit/browser jobs. Browser spot checks use explicit affected specs/tests with `--workers=1`; exercise desktop first unless the change is touch/mobile-specific, then check the affected mobile contract. Keep production build freshness explicit because Playwright serves `dist`.
- Prefer shared literal input/expected-output tables with Vitest `test.each` and uniquely named Playwright parameterized cases for genuinely equivalent behavior. Keep unique pixel effects, data-loss paths, ordering/races, and accessibility interactions independently asserted; do not derive expected results from the implementation being tested.
- Read executed counts, failures, skips, and exit status from runner output/reporters. Test totals are evidence, not capability-completion counts; sampled results must be labeled with their exact scope.
- Before closing a material implementation batch, perform one consolidated changed-file review, run all Photo unit/selector tests, rebuild production/PWA, and run every selected Photo browser spec across both configured projects. Rerun affected checks after review fixes; repeat the broader gate whenever the fix can invalidate it. Exact merge-state repository validation and deployment proof remain mandatory at release.
- For documentation-only cadence changes, validate the diff and referenced entrypoints; do not rerun the full browser matrix or imply fresh feature/build verification. The existing preset-transfer table was checked sequentially on 2026-09-15: 12 tests passed in one file; no new runtime, test configuration, dependency, or CI changes accompany this policy.
- Reassess cadence using completed capabilities, affected-run/full-gate duration, escaped defects, and coordination cost. If batching weakens failure attribution or defect detection, return the affected subsystem to immediate checks rather than weakening assertions or completion criteria.

The preview batch reused the existing decoder, native browser rasterization, and parameterized test tables without a new dependency or second decode path. Content-free, digest-bound accepted verification receipts were verified before unchanged-evidence reuse; changed source invalidated prior evidence. Git baseline plus scoped Photo diffs avoided rescanning unchanged files. No comparative token/time savings are claimed without telemetry. A build-time OneDrive placeholder fault in an existing unrelated dependency was resolved by restoring only its already-locked package from the official registry with lockfile-matching integrity; the original copy was retained recoverably, with no dependency upgrade or tracked-file change.

## Anti-staleness rule

- This tracker must be reviewed before and after each material Photo Studio implementation batch.
- When a gate changes from open to satisfied, update this file in the same branch before moving to a different release subsystem.
- CI evidence older than the current material implementation head is historical evidence only, not final completion evidence.
- A green PR branch is not equivalent to deployed completion; final evidence must come from the exact integrated `main` SHA.
- Parallel-agent branches are never deleted, force-moved, or rewritten as part of Photo Studio completion.
