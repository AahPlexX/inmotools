# Work Log

Completed tasks and material task-state reconciliations are summarized here with newest entries first.

- 2026-09-25 — Typing Workstation expanded and closed at 47/47. Added explicit Start/Pause/Resume/Stop/Reset lifecycle controls with pause-safe timing, persistent browser-local typist profiles, profile-scoped scores/history/PBs/exports, and profile-only score reset. Gauntlet then fixed active-session history mutations and the guarded New text affordance. Product tip `7222854833f507ebfbcf00ab0c156b58ee90f335`; dedicated run `36188765930` passed 76/76 focused units, production build, and 18/18 desktop/mobile browser checks; Pages run `36188765977` built and deployed the same revision successfully.
- 2026-09-24 — Vector Studio closed at 66/66. Reconciled historical Vector branches against current `main`, fixed focused validation so SVG changes run both `svg.spec.ts` and `vector-nested-composition.spec.ts`, then resolved the serious Axe `scrollable-region-focusable` defect with real keyboard-scrollable/focus-visible regions. Exact-main focused run `36052165692` passed build and 22/22 desktop/mobile Vector browser checks; Pages run `36052165503` passed repository units/build, built the Pages artifact, and deployed successfully. Static closure found no Vector TODO/FIXME/HACK or remote network path.
- 2026-09-24 — TASK-013 completed and the Markdown portion of TASK-014 reconciled: toolbar history now coalesces adjacent typing into meaningful document steps while CodeMirror keeps native fine-grained undo/caret behavior; live table-formula preparation moved to a reusable cancellable local Web Worker with stale-run protection and a no-Worker fallback. Red-first CI captured the missing runner and unwired browser behavior; final pre-integration PR run 36042091133 passed 154/154 unit files (1507/1507 tests), production build, and all 100 Markdown desktop/mobile browser checks.
- 2026-09-24 — Sightline Velocity UX/real-world remediation closed with F1–F35 preserved. PR #66 had already merged the completed reading-first UX/accessibility/responsive changes to `main`; tracker/handoff state was reconciled after dedicated run `35613856993` passed 409/409 focused units, production build, and 48/48 desktop/mobile browser checks, and Pages run `35613857161` built/deployed successfully. Static closure found no Sightline TODO/FIXME implementation debt or remote XHR/WebSocket path; the historical Sightline feature branch is 0 commits ahead of `main`.
- 2026-09-24 — Typing Workstation completion audit closed at 38/38. Replaced raw-keydown-only text capture with native input/composition handling for software keyboards and IMEs, restored physical key-code fidelity in raw logs, released Tab for standard focus traversal, moved fresh text to F2, added mobile Backspace handling, 24px tag-removal targets, and compact keyboard reflow. Exact-main run `36009754067` passed 67/67 focused units, production build, and 14/14 desktop/mobile browser checks; Pages run `36009753871` built and deployed successfully, with its 15 browser failures confined to unrelated workspaces/infrastructure while every Typing case passed.
- 2026-09-24 — Transcode Workstation closed at F01–F36 complete. PR #33 had already integrated the full implementation to `main`; this reconciliation removed its stale active-work entry and retired-branch instructions after fresh exact-main evidence showed unit tests and production build green, 22/22 Transcode browser cases executed across desktop/mobile with zero Transcode failures, and the Pages artifact/deploy jobs successful. No Transcode `FIXME` or remote network path was found; non-ledger legacy-format ideas remain documented as future scope rather than silently becoming completion blockers.
- 2026-09-11 — Completion/progress tracking hardened: added the deterministic repository completion contract and bound task-state freshness into `GOVERNANCE.md`; restored TASK-014 to `IN_PROGRESS.md`; reconciled the audit queue against current `main`; removed stale Convolution Room Profiler and Hardware lifecycle findings that were already fixed and evidenced by existing unit/browser coverage. Tool 17 / GLSL remediation remains the latest explicitly merged audit evidence, and TASK-014 stays open until every accepted finding has an explicit disposition plus final integrated validation/deployment evidence.
- 2026-09-05 — TASK-019 completed: verified a further forty-six audit findings and fixed the class that produces wrong or unsafe output. A base64-encoded request body was neither reported nor sanitized, so a credential survived a sanitization pass that reported success; the fluid type scale emitted mathematically incorrect CSS for every step except the base, and its four viewport previews all rendered at the real viewport width while claiming to show four; sprite compilation ignored colours declared in style attributes and dropped the coordinate system of any SVG lacking a viewBox; and the energy planner had no upper input bounds, so an absurd age produced a negative basal rate. Five findings did not reproduce and eleven were imprecise. Unit assertions 403 to 422.
- 2026-09-05 — TASK-018 completed: worked through a further twenty audit findings by verifying each against the code first. Nine did not reproduce and were left alone. Fixed the seven that did: the serial port was never closed so its stream locks persisted until the tab reloaded; GeoJSON simplification ran on the main thread; the MIDI progression was permanently four chords with no way to stop scheduled oscillators; the PDF queue had no remove control and every export collided on one filename; and the cron horizon was hardcoded to thirty runs. Also fixed the suite's long-standing floor-plan flake at its cause (TASK-017): the assertion waited on a worker-derived number, and the workspace now publishes an analysis-readiness signal derived during render. Unit assertions steady at 403, browser tests 222 to 230.
- 2026-09-04 — TASK-016 completed: removed the worst remaining failure mode from the audit backlog. Regex Log Structurer evaluated user-supplied patterns synchronously on the main thread, so a catastrophically backtracking pattern froze the tab with no way left to edit the pattern that caused it; structuring now runs in a worker behind a four-second deadline that terminates the run and names the cause. Added a shared PagedTable primitive and adopted it here, capping mounted rows while exports still cover the whole result. Added file import, a whole-document scan mode for records that span lines, honest flag gating, static named-group discovery, and column kind inference. Also reduced, but did not eliminate, the suite's flakiest assertion: it races geometry-worker startup rather than reporting a defect, and remains intermittent under full-suite contention (carried as TASK-017). Unit assertions 362 to 380, browser tests 208 to 216.
- 2026-09-04 — TASK-015 completed: fixed the crash-class and file-input defects surfaced by the catalog-wide audit. MIDI Harmony Lab called a throwing engine function during render against a free-text root, so deleting an octave blanked the workspace; Cron Team Matrix passed unvalidated free-text timezones to Intl.DateTimeFormat during render, where an unknown IANA name raises RangeError; and sixteen file inputs never cleared their value, so re-importing an edited file silently did nothing. Each fix carries a test proven to fail against the unfixed code. Findings that did not reproduce were deliberately left alone and recorded. Unit assertions 346 to 362; browser tests 202 to 208.
- 2026-09-04 — TASK-011 completed: Markdown Workbench completeness pass. Applied formatted in-text citations to the document (the formatting map was previously computed and discarded), unified the preview and all five exports onto one `prepareDocument` pipeline so they can no longer disagree, and fixed the empty-body export from Source view, the unconditional unsaved-changes prompt, the editor rebuild on every font-size step, the unscoped print output, and the uncancelled Graphviz workers. Wired previously unreachable code (`deleteDraft`, frontmatter, `renderMath`, storage quota, slide list) and added outline, math diagnostics, local file open, bibliography picker, document naming, and draft management. Unit suite 300 to 343 assertions; browser suite 180 to 202. Merged as pull request 8 and deployed.
- 2026-09-04 — TASK-010 completed: Markdown Workbench shipped as the twenty-sixth suite with GFM/CommonMark authoring, KaTeX math, Mermaid and Graphviz diagrams, spreadsheet-style table formulas, citeproc-js citations across four bundled CSL styles, and Markdown, standalone HTML, print-to-PDF, DOCX, structural EPUB, and AST JSON export. Merged as pull request 7 and deployed.
- 2026-09-04 — TASK-009 completed: restored GitHub Pages automatic redeployment. `main` had accumulated four consecutive failing validation runs, so no deployment had published since 2026-09-03. Root causes were a unit test committed without its implementation module, a commit that replaced the catalog and workspace registry with placeholder tokens, a follow-up fix that dropped the `energy-macro-planner` catalog entry while adding AetherCast, and a deployment-config test whose subject had not yet been changed. Merged as pull requests 4 and 6; deployment confirmed live.
- 2026-09-04 — TASK-003 and TASK-004 completed: trimmed the published payload. DuckDB WebAssembly and the Pyodide runtime are excluded from the service-worker precache and served through a runtime cache populated on first use, and published sourcemaps are disabled. First-visit precache fell from roughly 80 MB across 48 entries to roughly 11.5 MB (the entry count tracks however many chunks the catalog currently emits), and about 20 MB of `.map` files no longer ship. Asserted by `tests/unit/deployment-config.test.ts`.
- 2026-09-03 — TASK-008 completed: AetherCast shipped as the twenty-fifth suite, computing US EPA AQI, European EAQI, WHO 2021 guideline comparisons, and Fitzpatrick UV exposure estimates from a user-supplied hourly export.
- 2026-09-03 — TASK-007 completed: Energy & Macronutrient Planner shipped as the twenty-fourth suite, catalog-linked, route-aliased, and validated across five viewports.
- 2026-09-02 — TASK-002 completed: JSON Lattice Studio shipped, catalog-linked, route-aliased, unit- and browser-validated, and deployed; validation run 33587212287 succeeded.
- 2026-08-31 — TASK-001 completed: PlanCraft Studio implemented, catalog-linked, browser-validated, and deployed; validation run 33351325621 succeeded.
