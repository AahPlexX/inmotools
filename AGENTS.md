<!-- TASKPLANNER:START -->
Task planning is tracked in `.tasks/`. Before substantive implementation, move the selected highest-priority task from Next/Backlog to In Progress; when `aiPlanRequired` is enabled, add a concise `### Plan`. Preserve task IDs and unrelated task sections. After verified completion, replace the plan with a concise done-summary, move the full task section to Done, and add a short entry to `.tasks/WORK_LOG.md` when present.
<!-- TASKPLANNER:END -->
