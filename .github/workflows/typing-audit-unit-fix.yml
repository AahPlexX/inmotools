name: Typing audit unit one-shot fix

on:
  push:
    branches: [feature/typing-workstation]
    paths:
      - '.github/workflows/typing-audit-unit-fix.yml'

permissions:
  contents: write

jobs:
  patch-and-verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feature/typing-workstation
          fetch-depth: 0
      - uses: pnpm/setup@v2
        with:
          runtime: node@24
          install: false
      - run: pnpm install --frozen-lockfile
      - name: Apply audited source corrections
        shell: python
        run: |
          from pathlib import Path

          def replace_once(path: str, old: str, new: str) -> None:
              file = Path(path)
              text = file.read_text()
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f'{path}: expected 1 guarded match, found {count}')
              file.write_text(text.replace(old, new, 1))

          engine = 'src/tools/typing/typing-engine.ts'
          replace_once(engine, """  // Backspace handling.
  if (key === 'Backspace') {
    if (!opts.allowBackspace) return { ...state, backspaces: state.backspaces + 1 };
    const current = state.cells[state.cursor];
    if (current?.state === 'incorrect' && current.expected !== '') {
      const cells = state.cells.slice();
      cells[state.cursor] = { ...current, typed: '', state: 'pending', t: undefined };
      return { ...state, cells, backspaces: state.backspaces + 1 };
    }
    if (state.cursor === 0) return state;
    const next = { ...state, cells: state.cells.slice(), cursor: state.cursor - 1, backspaces: state.backspaces + 1 };
    const idx = next.cursor;
    const cell = next.cells[idx];
    if (cell) {
      // If this is a synthetic \"extra\" cell, drop it entirely.
      if (cell.expected === '' && cell.state === 'extra') {
        next.cells.splice(idx, 1);
      } else {
        next.cells[idx] = { ...cell, typed: '', state: 'pending', t: undefined };
      }
    }
    return next;
  }
""", """  // Backspace handling. Backspace remains outside the accuracy denominator, but
  // once a test has started it is retained in the raw event log so ghost replay
  // can reverse progress that was actually erased.
  if (key === 'Backspace') {
    const appendBackspace = (index: number, expected: string): KeystrokeEvent[] => (
      state.startedAt == null
        ? state.events
        : [...state.events, { t, key, code, correct: false, index, expected }]
    );
    if (!opts.allowBackspace) {
      return { ...state, events: appendBackspace(-1, ''), backspaces: state.backspaces + 1 };
    }
    const current = state.cells[state.cursor];
    if (current?.state === 'incorrect' && current.expected !== '') {
      const cells = state.cells.slice();
      cells[state.cursor] = { ...current, typed: '', state: 'pending', t: undefined };
      return {
        ...state,
        cells,
        events: appendBackspace(state.cursor, current.expected),
        backspaces: state.backspaces + 1,
      };
    }
    if (state.cursor === 0) {
      return { ...state, events: appendBackspace(-1, ''), backspaces: state.backspaces + 1 };
    }
    const erasedIndex = state.cursor - 1;
    const erased = state.cells[erasedIndex];
    const next = {
      ...state,
      events: appendBackspace(erasedIndex, erased?.expected ?? ''),
      cells: state.cells.slice(),
      cursor: erasedIndex,
      backspaces: state.backspaces + 1,
    };
    const cell = next.cells[erasedIndex];
    if (cell) {
      // If this is a synthetic \"extra\" cell, drop it entirely.
      if (cell.expected === '' && cell.state === 'extra') {
        next.cells.splice(erasedIndex, 1);
      } else {
        next.cells[erasedIndex] = { ...cell, typed: '', state: 'pending', t: undefined };
      }
    }
    return next;
  }
""")

          replace_once(engine, """export function wpmSeries(state: EngineState, endT?: number): WpmSample[] {
  const start = state.startedAt;
  if (start == null) return [];
  const end = state.endedAt ?? endT ?? start;
  const totalSecs = Math.max(1, Math.ceil((end - start) / 1000));
  const buckets: { correct: number; total: number; errors: number }[] = [];
  for (let i = 0; i < totalSecs; i += 1) buckets.push({ correct: 0, total: 0, errors: 0 });
  for (const e of state.events) {
    if (e.key.length !== 1) continue;
    const s = Math.min(totalSecs - 1, Math.max(0, Math.floor((e.t - start) / 1000)));
    const b = buckets[s]!;
    b.total += 1;
    if (e.correct) b.correct += 1;
    else b.errors += 1;
  }
  const samples: WpmSample[] = [];
  let cumulativeCorrect = 0;
  let cumulativeTotal = 0;
  for (let i = 0; i < buckets.length; i += 1) {
    const b = buckets[i]!;
    cumulativeCorrect += b.correct;
    cumulativeTotal += b.total;
    const seconds = i + 1;
    const minutes = seconds / 60;
    const wpm = (cumulativeCorrect / STANDARD_WORD) / minutes;
    const rawWpm = (cumulativeTotal / STANDARD_WORD) / minutes;
    samples.push({ seconds, wpm: round(wpm), rawWpm: round(rawWpm), errorsInWindow: b.errors });
  }
  return samples;
}
""", """function isCommittedTextEvent(event: KeystrokeEvent): boolean {
  return event.key.length === 1 || (event.key === 'Enter' && event.expected === '\\n');
}

export function wpmSeries(state: EngineState, endT?: number): WpmSample[] {
  const start = state.startedAt;
  if (start == null) return [];
  const end = state.endedAt ?? endT ?? start;
  const totalSecs = Math.max(1, Math.ceil((end - start) / 1000));
  const errorsBySecond = Array.from({ length: totalSecs }, () => 0);
  for (const event of state.events) {
    if (!isCommittedTextEvent(event) || event.correct) continue;
    const bucket = Math.min(totalSecs - 1, Math.max(0, Math.floor((event.t - start) / 1000)));
    errorsBySecond[bucket] = (errorsBySecond[bucket] ?? 0) + 1;
  }

  const samples: WpmSample[] = [];
  const correctIndexes = new Set<number>();
  let cumulativeTotal = 0;
  let cursor = 0;
  for (let second = 1; second <= totalSecs; second += 1) {
    const cutoff = start + second * 1000;
    while (cursor < state.events.length && state.events[cursor]!.t <= cutoff) {
      const event = state.events[cursor]!;
      if (event.key === 'Backspace') {
        if (event.index >= 0) correctIndexes.delete(event.index);
      } else if (isCommittedTextEvent(event)) {
        cumulativeTotal += 1;
        if (event.correct) correctIndexes.add(event.index);
      }
      cursor += 1;
    }
    const minutes = second / 60;
    const wpm = (correctIndexes.size / STANDARD_WORD) / minutes;
    const rawWpm = (cumulativeTotal / STANDARD_WORD) / minutes;
    samples.push({
      seconds: second,
      wpm: round(wpm),
      rawWpm: round(rawWpm),
      errorsInWindow: errorsBySecond[second - 1] ?? 0,
    });
  }
  return samples;
}
""")

          replace_once(engine, """export function ghostSeries(events: KeystrokeEvent[]): GhostSample[] {
  if (events.length === 0) return [];
  const start = events[0]!.t;
  const end = events[events.length - 1]!.t;
  const totalSecs = Math.max(1, Math.ceil((end - start) / 1000));
  const samples: GhostSample[] = [];
  let correct = 0;
  let cursor = 0;
  for (let s = 1; s <= totalSecs; s += 1) {
    const cutoff = start + s * 1000;
    while (cursor < events.length && events[cursor]!.t <= cutoff) {
      const ev = events[cursor]!;
      if (ev.key.length === 1 && ev.correct) correct += 1;
      cursor += 1;
    }
    samples.push({ seconds: s, correctChars: correct });
  }
  return samples;
}
""", """export function ghostSeries(events: KeystrokeEvent[]): GhostSample[] {
  if (events.length === 0) return [];
  const start = events[0]!.t;
  const end = events[events.length - 1]!.t;
  const totalSecs = Math.max(1, Math.ceil((end - start) / 1000));
  const samples: GhostSample[] = [];
  const correctIndexes = new Set<number>();
  let cursor = 0;
  for (let s = 1; s <= totalSecs; s += 1) {
    const cutoff = start + s * 1000;
    while (cursor < events.length && events[cursor]!.t <= cutoff) {
      const event = events[cursor]!;
      if (event.key === 'Backspace') {
        if (event.index >= 0) correctIndexes.delete(event.index);
      } else if (isCommittedTextEvent(event) && event.correct) {
        correctIndexes.add(event.index);
      }
      cursor += 1;
    }
    samples.push({ seconds: s, correctChars: correctIndexes.size });
  }
  return samples;
}
""")

          audio = 'src/tools/typing/typing-audio.ts'
          replace_once(audio, """export interface AudioController {
""", """export type KeystrokeSound = 'correct' | 'incorrect' | 'space' | 'enter' | 'backspace';

export function classifyKeystrokeSound(
  key: string,
  expected: string | undefined,
  caseSensitive: boolean,
): KeystrokeSound | null {
  if (key === 'Backspace') return 'backspace';
  if (key === 'Enter') return expected === '\\n' ? 'enter' : null;
  if (key.length !== 1 || expected === undefined) return null;
  const actualValue = caseSensitive ? key : key.toLocaleLowerCase();
  const expectedValue = caseSensitive ? expected : expected.toLocaleLowerCase();
  if (actualValue !== expectedValue) return 'incorrect';
  return key === ' ' ? 'space' : 'correct';
}

export interface AudioController {
""")

          corpora = 'src/tools/typing/typing-corpora.ts'
          replace_once(corpora, """export function findLayout(id: LayoutId): LayoutDefinition {
  const found = LAYOUTS.find((l) => l.id === id);
  if (!found) throw new Error(`Unknown layout: ${id}`);
  return { ...found, fingers: fingersForRows(found.rows) };
}
""", """export function findLayout(id: LayoutId): LayoutDefinition {
  const found = LAYOUTS.find((l) => l.id === id);
  if (!found) throw new Error(`Unknown layout: ${id}`);
  return { ...found, fingers: fingersForRows(found.rows) };
}

export function homeRowAnchors(layout: LayoutDefinition): { left: string; right: string } {
  const homeRow = layout.rows[2]?.keys ?? [];
  const left = homeRow.find((key) => layout.fingers[key] === 'l2') ?? 'F';
  const right = homeRow.slice().reverse().find((key) => layout.fingers[key] === 'r2') ?? 'J';
  return { left, right };
}
""")

          storage = 'src/tools/typing/typing-storage.ts'
          replace_once(storage, """import type { CorpusMode, Language, LayoutId, Quote } from './typing-corpora';
""", """import { LANGUAGE_POOLS, LAYOUTS, type CorpusMode, type Language, type LayoutId, type Quote } from './typing-corpora';
""")
          replace_once(storage, """// --------------------------------------------------------------------
// Tests
// --------------------------------------------------------------------
export async function saveTest(test: StoredTest): Promise<number> {
  const localTest = { ...test };
  delete localTest.id;
  return getDb().tests.add(localTest);
}
""", """// --------------------------------------------------------------------
// Tests
// --------------------------------------------------------------------
const CORPUS_MODES = new Set<CorpusMode>([
  'words-200', 'words-1000', 'words-5000', 'punctuation', 'numbers', 'code',
  'medical', 'legal', 'kids', 'quote', 'zen', 'custom',
]);
const DURATION_MODES = new Set<StoredTest['durationMode']>(['time', 'words', 'quote', 'zen', 'certification']);
const FINISH_REASONS = new Set<StoredTest['finishReason']>(['completed', 'failed', 'aborted']);
const QUOTE_LENGTHS = new Set<Quote['length']>(['short', 'medium', 'long', 'thicc']);
const LANGUAGES = new Set<Language>(Object.keys(LANGUAGE_POOLS) as Language[]);
const LAYOUT_IDS = new Set<LayoutId>(LAYOUTS.map((layout) => layout.id));

function finiteNumber(value: unknown, min = 0, max = Number.POSITIVE_INFINITY): value is number {
  return typeof value === 'number' && Number.isFinite(value) && value >= min && value <= max;
}

function validKeystrokes(value: unknown): value is KeystrokeEvent[] {
  if (!Array.isArray(value)) return false;
  return value.every((event) => {
    if (event == null || typeof event !== 'object') return false;
    const record = event as Partial<KeystrokeEvent>;
    return finiteNumber(record.t)
      && typeof record.key === 'string'
      && typeof record.code === 'string'
      && typeof record.correct === 'boolean'
      && Number.isInteger(record.index)
      && typeof record.expected === 'string';
  });
}

export function normalizeStoredTest(value: unknown, fallbackSavedAt = Date.now()): StoredTest | null {
  if (value == null || typeof value !== 'object') return null;
  const record = value as Partial<StoredTest>;
  if (!CORPUS_MODES.has(record.mode as CorpusMode)) return null;
  if (!DURATION_MODES.has(record.durationMode as StoredTest['durationMode'])) return null;
  if (!LANGUAGES.has(record.language as Language) || !LAYOUT_IDS.has(record.layout as LayoutId)) return null;
  if (!FINISH_REASONS.has(record.finishReason as StoredTest['finishReason'])) return null;
  if (record.quoteLength !== undefined && !QUOTE_LENGTHS.has(record.quoteLength)) return null;
  if (typeof record.targetText !== 'string') return null;
  if (!finiteNumber(record.durationValue)) return null;
  if (!finiteNumber(record.netWpm) || !finiteNumber(record.grossWpm) || !finiteNumber(record.rawCpm)) return null;
  if (!finiteNumber(record.accuracy, 0, 100) || !finiteNumber(record.consistency, 0, 100)) return null;
  if (!finiteNumber(record.elapsedMs) || !finiteNumber(record.correctChars) || !finiteNumber(record.incorrectChars)
      || !finiteNumber(record.missedChars) || !finiteNumber(record.extraChars)) return null;
  if (record.tags !== undefined && (!Array.isArray(record.tags) || !record.tags.every((tag) => typeof tag === 'string'))) return null;
  if (record.notes !== undefined && typeof record.notes !== 'string') return null;
  if (record.keystrokes !== undefined && !validKeystrokes(record.keystrokes)) return null;
  const savedAt = record.savedAt === undefined ? fallbackSavedAt : record.savedAt;
  if (!finiteNumber(savedAt)) return null;
  if (record.id !== undefined && (!Number.isInteger(record.id) || record.id <= 0)) return null;

  return {
    ...(record as StoredTest),
    savedAt,
    tags: record.tags ?? [],
    notes: record.notes ?? '',
  };
}

export async function saveTest(test: StoredTest): Promise<number> {
  const normalized = normalizeStoredTest(test);
  if (!normalized) throw new Error('Invalid typing test record.');
  const localTest = { ...normalized };
  delete localTest.id;
  return getDb().tests.add(localTest);
}
""")
          replace_once(storage, """export async function filterTests(opts: {
  tags?: string[];
  mode?: CorpusMode;
  language?: Language;
  layout?: LayoutId;
  since?: number;
  until?: number;
}): Promise<StoredTest[]> {
  const all = await listTests();
  return all.filter((t) => {
    if (opts.mode && t.mode !== opts.mode) return false;
    if (opts.language && t.language !== opts.language) return false;
    if (opts.layout && t.layout !== opts.layout) return false;
    if (opts.since && t.savedAt < opts.since) return false;
    if (opts.until && t.savedAt > opts.until) return false;
    if (opts.tags && opts.tags.length > 0) {
      if (!opts.tags.every((tag) => t.tags.includes(tag))) return false;
    }
    return true;
  });
}
""", """export interface TestFilterOptions {
  tags?: string[];
  mode?: CorpusMode;
  language?: Language;
  layout?: LayoutId;
  since?: number;
  until?: number;
}

export function filterStoredTests(tests: StoredTest[], opts: TestFilterOptions): StoredTest[] {
  return tests.filter((test) => {
    if (opts.mode && test.mode !== opts.mode) return false;
    if (opts.language && test.language !== opts.language) return false;
    if (opts.layout && test.layout !== opts.layout) return false;
    if (opts.since && test.savedAt < opts.since) return false;
    if (opts.until && test.savedAt > opts.until) return false;
    if (opts.tags && opts.tags.length > 0 && !opts.tags.every((tag) => test.tags.includes(tag))) return false;
    return true;
  });
}

export async function filterTests(opts: TestFilterOptions): Promise<StoredTest[]> {
  return filterStoredTests(await listTests(), opts);
}
""")
          replace_once(storage, """      t.durationMode === query.durationMode
      && t.durationValue === query.durationValue
      && t.language === query.language
""", """      t.durationMode === query.durationMode
      && (query.durationMode === 'quote' || t.durationValue === query.durationValue)
      && t.language === query.language
""")

          export_file = 'src/tools/typing/typing-export.ts'
          replace_once(export_file, """import type { StoredTest } from './typing-storage';
""", """import { normalizeStoredTest, type StoredTest } from './typing-storage';
""")
          replace_once(export_file, """  return Papa.unparse(rows, { header: true, newline: '\\n' });
}

export function keystrokesToCsv""", """  return Papa.unparse(rows, { header: true, newline: '\\n', escapeFormulae: true });
}

export function keystrokesToCsv""")
          replace_once(export_file, """    { header: true, newline: '\\n' },
  );
}

// --------------------------------------------------------------------
// JSON
""", """    { header: true, newline: '\\n', escapeFormulae: true },
  );
}

// --------------------------------------------------------------------
// JSON
""")
          replace_once(export_file, """export function testsToJson(tests: StoredTest[], meta: ExportMetadata): string {
""", """export function parseImportedTests(value: unknown, now = Date.now()): { tests: StoredTest[]; skipped: number } {
  if (value == null || typeof value !== 'object') return { tests: [], skipped: 0 };
  const envelope = value as { tool?: unknown; schemaVersion?: unknown; tests?: unknown; test?: unknown };
  if (envelope.tool !== undefined && envelope.tool !== 'inmotools-typing-workstation') return { tests: [], skipped: 0 };
  if (envelope.schemaVersion !== undefined && envelope.schemaVersion !== 1) return { tests: [], skipped: 0 };
  const candidates = Array.isArray(envelope.tests) ? envelope.tests : envelope.test !== undefined ? [envelope.test] : [];
  const tests: StoredTest[] = [];
  let skipped = 0;
  for (const candidate of candidates) {
    const normalized = normalizeStoredTest(candidate, now);
    if (normalized) tests.push(normalized);
    else skipped += 1;
  }
  return { tests, skipped };
}

export function testsToJson(tests: StoredTest[], meta: ExportMetadata): string {
""")
          replace_once(export_file, """export function certificatePdf(test: StoredTest, meta: ExportMetadata): Blob {
  const doc = new jsPDF({ orientation: 'landscape', unit: 'pt', format: 'letter' });
""", """export function certificatePdf(test: StoredTest, meta: ExportMetadata): Blob {
  if (test.finishReason !== 'completed') {
    throw new Error('Proficiency certificates require a completed test.');
  }
  const doc = new jsPDF({ orientation: 'landscape', unit: 'pt', format: 'letter' });
""")

          target = 'src/tools/typing/typing-target.ts'
          replace_once(target, """    case 'custom':
      source = cfg.customText.trim();
      break;
""", """    case 'custom':
      source = cfg.customText.replace(/\\t/g, '    ').trim();
      break;
""")
      - name: Focused typing unit tests
        run: pnpm exec vitest run tests/unit/typing-*.test.ts
      - name: Commit audited unit corrections
        run: |
          git config user.name "AahPlexX"
          git config user.email "130876266+AahPlexX@users.noreply.github.com"
          git add src/tools/typing/typing-engine.ts src/tools/typing/typing-audio.ts src/tools/typing/typing-corpora.ts src/tools/typing/typing-export.ts src/tools/typing/typing-storage.ts src/tools/typing/typing-target.ts
          git diff --cached --quiet && exit 0
          git commit -m "fix(typing): resolve audited data and export defects [skip ci]"
          git push origin HEAD:feature/typing-workstation
