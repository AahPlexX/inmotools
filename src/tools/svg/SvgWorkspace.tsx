import { useMemo, useState } from 'react';
import { downloadText } from '../../lib/download';
import { compileSvgSprite } from './svg-engine';
import { consumeFileInput } from '../../lib/file-input';

type Source = { name: string; text: string };

export default function SvgWorkspace(){
 const[sources,setSources]=useState<Source[]>([]);const[currentColor,setCurrentColor]=useState(true);const[result,setResult]=useState<ReturnType<typeof compileSvgSprite>|null>(null);const[status,setStatus]=useState('Choose SVG files to inspect and compile.');const[search,setSearch]=useState('');
 const visibleFiles=useMemo(()=>{const query=search.trim().toLowerCase();if(!result||!query)return result?.files??[];return result.files.filter(file=>file.name.toLowerCase().includes(query)||file.id.toLowerCase().includes(query))},[result,search]);
 async function load(list:FileList|null){if(!list)return;const next=await Promise.all(Array.from(list).map(async file=>({name:file.name,text:await file.text()})));setSources(next);setResult(null);setSearch('');setStatus(`${next.length} SVG${next.length===1?'':'s'} ready for local optimization.`)}
 function updateCurrentColor(value:boolean){setCurrentColor(value);setResult(null);setStatus('Normalization changed. Recompile to generate output with the new setting.')}
 function compile(){const output=compileSvgSprite(sources,{currentColor});setResult(output);if(output.errors.length&&output.files.length)setStatus(`Compiled ${output.files.length} symbol${output.files.length===1?'':'s'}; ${output.errors.length} file${output.errors.length===1?'':'s'} could not be compiled.`);else if(output.errors.length)setStatus(`No symbols compiled. ${output.errors.length} file${output.errors.length===1?'':'s'} failed validation.`);else setStatus(`Compiled ${output.files.length} symbols locally.`)}
 async function copyUse(id:string){try{await navigator.clipboard.writeText(`<svg aria-hidden="true"><use href="#${id}" /></svg>`);setStatus(`Copied <use> snippet for ${id}.`)}catch{setStatus('Clipboard access was unavailable. Select the sprite output and copy manually.')}}
 return <><div className="workspace-header"><div><h2>Sprite compiler</h2><p>Each symbol and every internal paint/reference ID is made deterministic and collision-safe.</p></div></div><div className="workspace-body"><div className="field"><label htmlFor="svg-files">Choose SVG files</label><input id="svg-files" type="file" accept="image/svg+xml,.svg" multiple onChange={e=>consumeFileInput(e.target,()=>load(e.target.files))}/></div><label style={{display:'flex',gap:10,alignItems:'center',marginTop:16}}><input type="checkbox" checked={currentColor} onChange={e=>updateCurrentColor(e.target.checked)}/> Normalize literal fill/stroke colors to currentColor</label><p className="help-text">Paint-server references such as <code>url(#gradient)</code>, <code>none</code>, and existing <code>currentColor</code> values are preserved.</p><div className="button-row"><button className="action-button" type="button" disabled={!sources.length} onClick={compile}>Compile sprite</button><button className="action-button secondary" type="button" disabled={!result?.files.length} onClick={()=>result&&downloadText(result.sprite,'inmotools-sprite.svg','image/svg+xml')}>Download sprite</button></div><div className="status-line" role="status">{status}</div>{result?<>{result.errors.length?<div className="notice" data-testid="svg-errors"><strong>Files not compiled</strong><ul>{result.errors.map(error=><li key={`${error.name}-${error.message}`}><strong>{error.name}:</strong> {error.message}</li>)}</ul></div>:null}{result.files.length?<><div className="field" style={{marginTop:16,maxWidth:420}}><label htmlFor="svg-search">Search compiled symbols</label><input id="svg-search" type="search" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Filename or symbol ID"/></div><div aria-label="Compiled symbol previews" className="workspace-grid" style={{marginTop:16}}>{visibleFiles.map(file=><article className="notice" key={file.id} style={{minWidth:0}}><strong style={{overflowWrap:'anywhere'}}>{file.name}</strong><div className="help-text" style={{overflowWrap:'anywhere'}}>{file.id}</div><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(120px,1fr))',gap:10,marginTop:10}}><div style={{background:'#fff',color:'#111',padding:12,borderRadius:10,textAlign:'center'}}><span className="help-text">Light</span><div dangerouslySetInnerHTML={{__html:`${result.sprite}<svg viewBox="0 0 32 32" width="64" height="64" aria-label="${file.id} light preview"><use href="#${file.id}" /></svg>`}}/></div><div style={{background:'#111',color:'#fff',padding:12,borderRadius:10,textAlign:'center'}}><span className="help-text">Dark</span><div dangerouslySetInnerHTML={{__html:`${result.sprite}<svg viewBox="0 0 32 32" width="64" height="64" aria-label="${file.id} dark preview"><use href="#${file.id}" /></svg>`}}/></div></div><button className="action-button secondary" type="button" style={{marginTop:10}} onClick={()=>void copyUse(file.id)}>Copy &lt;use&gt;</button></article>)}</div><div className="result-table-wrap" role="region" aria-label="Compiled SVG symbols" tabIndex={0}><table><thead><tr><th scope="col">File</th><th scope="col">Symbol ID</th><th scope="col">Original</th><th scope="col">Optimized</th><th scope="col">Savings</th></tr></thead><tbody>{visibleFiles.map(file=><tr key={file.id}><td>{file.name}</td><td>{file.id}</td><td>{file.originalBytes} B</td><td>{file.optimizedBytes} B</td><td>{file.originalBytes?Math.max(0,Math.round((1-file.optimizedBytes/file.originalBytes)*100)):0}%</td></tr>)}</tbody></table></div><pre className="code-output" tabIndex={0} aria-label="Compiled SVG sprite source">{result.sprite}</pre></>:null}</>:null}</div></>}
