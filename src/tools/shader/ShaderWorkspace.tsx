import { useEffect, useRef, useState } from 'react';
import { downloadText } from '../../lib/download';
import ShaderEditor from './ShaderEditor';
import { buildStandaloneShaderHtml, normalizeRenderScale, parseWebGlLog, type ShaderDiagnostic } from './shader-engine';
import { consumeFileInput } from '../../lib/file-input';

const DEFAULT_SOURCE = `#version 300 es
precision highp float;
out vec4 outColor;
uniform vec2 u_resolution;
uniform float u_time;
uniform vec2 u_mouse;
uniform sampler2D u_texture0;
uniform sampler2D u_texture1;
void main(){vec2 uv=gl_FragCoord.xy/max(u_resolution,vec2(1.0));float pulse=.5+.5*sin(u_time*1.8);outColor=vec4(uv.x,.22+uv.y*.55,.72+pulse*.28,1.0);}`;
const VERTEX_SOURCE=`#version 300 es
const vec2 positions[3]=vec2[3](vec2(-1.,-1.),vec2(3.,-1.),vec2(-1.,3.));
void main(){gl_Position=vec4(positions[gl_VertexID],0.,1.);}`;
type LocalTexture={name:string;dataUrl:string}|null;
const fileToDataUrl=(file:File)=>new Promise<string>((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>typeof reader.result==='string'?resolve(reader.result):reject(new Error('Image could not be encoded.'));reader.onerror=()=>reject(reader.error??new Error('Image read failed.'));reader.readAsDataURL(file);});

export default function ShaderWorkspace(){
 const canvasRef=useRef<HTMLCanvasElement>(null);const clockStartRef=useRef(performance.now());
 const [source,setSource]=useState(DEFAULT_SOURCE),[compiledSource,setCompiledSource]=useState(DEFAULT_SOURCE);const [textures,setTextures]=useState<LocalTexture[]>([null,null]);const [diagnostics,setDiagnostics]=useState<ShaderDiagnostic[]>([]);const [webgl2,setWebgl2]=useState(true),[autoCompile,setAutoCompile]=useState(true),[paused,setPaused]=useState(false);const [elapsed,setElapsed]=useState(0),[renderScale,setRenderScale]=useState(1),[focusLine,setFocusLine]=useState<number>();const [view,setView]=useState<'split'|'editor'|'preview'>('split');const [status,setStatus]=useState('Editing and rendering happen locally with WebGL2.');
 useEffect(()=>{if(!autoCompile)return;const id=window.setTimeout(()=>setCompiledSource(source),350);return()=>clearTimeout(id);},[autoCompile,source]);
 useEffect(()=>{const canvas=canvasRef.current;if(!canvas||view==='editor')return;const gl=canvas.getContext('webgl2',{antialias:true});if(!gl){setWebgl2(false);setDiagnostics([]);setStatus('WebGL2 is not available in this browser or graphics environment.');return;}setWebgl2(true);let animation:number|null=null,disposed=false;const found:ShaderDiagnostic[]=[];const compile=(type:number,text:string)=>{const shader=gl.createShader(type);if(!shader)return null;gl.shaderSource(shader,text);gl.compileShader(shader);const log=gl.getShaderInfoLog(shader)??'';if(log.trim())found.push(...parseWebGlLog(log));return shader;};const vertex=compile(gl.VERTEX_SHADER,VERTEX_SOURCE),fragment=compile(gl.FRAGMENT_SHADER,compiledSource);if(!vertex||!fragment||!gl.getShaderParameter(vertex,gl.COMPILE_STATUS)||!gl.getShaderParameter(fragment,gl.COMPILE_STATUS)){setDiagnostics(found);setStatus('Shader compilation failed. Select a diagnostic to jump to its line.');if(vertex)gl.deleteShader(vertex);if(fragment)gl.deleteShader(fragment);return;}const program=gl.createProgram();if(!program)return;gl.attachShader(program,vertex);gl.attachShader(program,fragment);gl.linkProgram(program);const linkLog=gl.getProgramInfoLog(program)??'';if(linkLog.trim())found.push(...parseWebGlLog(linkLog));if(!gl.getProgramParameter(program,gl.LINK_STATUS)){setDiagnostics(found);setStatus('Shader linking failed.');gl.deleteProgram(program);gl.deleteShader(vertex);gl.deleteShader(fragment);return;}gl.useProgram(program);setDiagnostics(found);setStatus(found.length?`Shader linked with ${found.length} compiler message${found.length===1?'':'s'}.`:'Shader compiled and linked successfully.');const resolution=gl.getUniformLocation(program,'u_resolution'),time=gl.getUniformLocation(program,'u_time'),mouse=gl.getUniformLocation(program,'u_mouse'),textureUniforms=[gl.getUniformLocation(program,'u_texture0'),gl.getUniformLocation(program,'u_texture1')];const handles:WebGLTexture[]=[];const pointer={x:0,y:0};for(let index=0;index<2;index++){const handle=gl.createTexture();if(!handle)continue;handles.push(handle);gl.activeTexture(gl.TEXTURE0+index);gl.bindTexture(gl.TEXTURE_2D,handle);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_WRAP_S,gl.CLAMP_TO_EDGE);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_WRAP_T,gl.CLAMP_TO_EDGE);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_MIN_FILTER,gl.LINEAR);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_MAG_FILTER,gl.LINEAR);gl.texImage2D(gl.TEXTURE_2D,0,gl.RGBA,1,1,0,gl.RGBA,gl.UNSIGNED_BYTE,new Uint8Array([0,0,0,255]));if(textureUniforms[index]!==null)gl.uniform1i(textureUniforms[index],index);const local=textures[index];if(local){const image=new Image();image.onload=()=>{if(disposed)return;gl.activeTexture(gl.TEXTURE0+index);gl.bindTexture(gl.TEXTURE_2D,handle);gl.pixelStorei(gl.UNPACK_FLIP_Y_WEBGL,true);gl.texImage2D(gl.TEXTURE_2D,0,gl.RGBA,gl.RGBA,gl.UNSIGNED_BYTE,image);};image.src=local.dataUrl;}}
 const onPointer=(event:PointerEvent)=>{const rect=canvas.getBoundingClientRect();pointer.x=(event.clientX-rect.left)*(canvas.width/Math.max(1,rect.width));pointer.y=(rect.bottom-event.clientY)*(canvas.height/Math.max(1,rect.height));};canvas.addEventListener('pointermove',onPointer,{passive:true});const resize=()=>{const scale=Math.max(.25,Math.min(2,(window.devicePixelRatio||1)*normalizeRenderScale(renderScale)));const width=Math.max(1,Math.round(canvas.clientWidth*scale)),height=Math.max(1,Math.round(canvas.clientHeight*scale));if(canvas.width!==width||canvas.height!==height){canvas.width=width;canvas.height=height;}gl.viewport(0,0,canvas.width,canvas.height);};const draw=(now:number)=>{resize();gl.useProgram(program);if(resolution!==null)gl.uniform2f(resolution,canvas.width,canvas.height);if(time!==null)gl.uniform1f(time,paused?elapsed:elapsed+(now-clockStartRef.current)/1000);if(mouse!==null)gl.uniform2f(mouse,pointer.x,pointer.y);gl.drawArrays(gl.TRIANGLES,0,3);if(!paused)animation=requestAnimationFrame(draw);};animation=requestAnimationFrame(draw);return()=>{disposed=true;if(animation!==null)cancelAnimationFrame(animation);canvas.removeEventListener('pointermove',onPointer);handles.forEach((texture)=>gl.deleteTexture(texture));gl.deleteProgram(program);gl.deleteShader(vertex);gl.deleteShader(fragment);};},[compiledSource,elapsed,paused,renderScale,textures,view]);
 const togglePause=()=>{if(paused){clockStartRef.current=performance.now();setPaused(false);}else{setElapsed((value)=>value+(performance.now()-clockStartRef.current)/1000);setPaused(true);}};const resetTime=()=>{setElapsed(0);clockStartRef.current=performance.now();};
 async function loadTexture(file:File|undefined,index:number){if(!file)return;try{const dataUrl=await fileToDataUrl(file);setTextures((current)=>current.map((item,i)=>i===index?{name:file.name,dataUrl}:item));setStatus(`${file.name} ready for u_texture${index}.`);}catch(error){setStatus(`Texture load failed: ${error instanceof Error?error.message:'unsupported image'}`);}}
 const exportHtml=()=>{downloadText(buildStandaloneShaderHtml({fragmentSource:compiledSource,textureDataUrls:textures.map((texture)=>texture?.dataUrl??''),renderScale}),'shader-demo.html','text/html;charset=utf-8');setStatus('Exported standalone WebGL2 HTML with preview-equivalent placeholder textures.');};
 return <><div className="workspace-header"><div><h2>Live GLSL sandbox</h2><p>Compile fragment source locally, inspect diagnostics, test uniforms/textures, and export a standalone WebGL2 page.</p></div></div><div className="workspace-body"><div className="button-row" role="group" aria-label="Shader workspace controls"><button className="action-button secondary" type="button" onClick={()=>setView('editor')} aria-pressed={view==='editor'}>Editor</button><button className="action-button secondary" type="button" onClick={()=>setView('split')} aria-pressed={view==='split'}>Split</button><button className="action-button secondary" type="button" onClick={()=>setView('preview')} aria-pressed={view==='preview'}>Preview</button><button className="action-button secondary" type="button" onClick={togglePause}>{paused?'Resume time':'Pause time'}</button><button className="action-button secondary" type="button" onClick={resetTime}>Reset time</button><button className="action-button" type="button" onClick={()=>setCompiledSource(source)}>Compile now</button></div><div className="workspace-grid three" style={{marginTop:16}}><label style={{display:'flex',gap:8,alignItems:'center'}}><input type="checkbox" checked={autoCompile} onChange={(event)=>setAutoCompile(event.target.checked)}/> Auto-compile after 350 ms</label><div className="field"><label htmlFor="shader-scale">Render scale</label><input id="shader-scale" type="number" min="0.25" max="2" step="0.25" value={renderScale} onChange={(event)=>setRenderScale(normalizeRenderScale(Number(event.target.value)))}/></div></div><div className={view==='split'?'workspace-grid':undefined}>{view!=='preview'?<div className="field"><span className="field-label">Fragment shader</span><ShaderEditor value={source} onChange={setSource} focusLine={focusLine}/><small>{compiledSource===source?'Preview matches editor.':'Editor has uncompiled changes.'}</small></div>:null}{view!=='editor'?<div><div className="workspace-grid">{[0,1].map((index)=><div className="field" key={index}><label htmlFor={`shader-texture-${index}`}>u_texture{index}</label><input id={`shader-texture-${index}`} type="file" accept="image/*" onChange={(event)=>consumeFileInput(event.target,()=>loadTexture(event.target.files?.[0],index))}/>{textures[index]?<button type="button" className="action-button secondary" onClick={()=>setTextures((current)=>current.map((item,i)=>i===index?null:item))}>Remove texture {index}</button>:<small>Black 1×1 placeholder, matching exported HTML.</small>}</div>)}</div><canvas ref={canvasRef} tabIndex={0} aria-label="Live WebGL2 fragment shader preview" style={{display:'block',width:'100%',height:420,marginTop:18,border:'1px solid var(--line)',borderRadius:'var(--radius-sm)',background:'#000',touchAction:'none'}}/>{!webgl2?<div className="notice" style={{marginTop:14}}>WebGL2 is unavailable here. Editing/export remain available.</div>:null}</div>:null}</div><div className="button-row"><button className="action-button" type="button" onClick={exportHtml}>Export standalone HTML</button><button className="action-button secondary" type="button" onClick={()=>{setSource(DEFAULT_SOURCE);setCompiledSource(DEFAULT_SOURCE);setTextures([null,null]);resetTime();setStatus('Restored the local starter shader.');}}>Reset starter shader</button></div><h3 style={{marginTop:24}}>Compiler diagnostics</h3>{diagnostics.length?<div className="result-table-wrap" tabIndex={0} aria-label="Shader compiler diagnostics"><table><thead><tr><th>Severity</th><th>Line</th><th>Column</th><th>Message</th></tr></thead><tbody>{diagnostics.map((diagnostic,index)=><tr key={`${diagnostic.line}-${diagnostic.column??0}-${index}`}><td>{diagnostic.severity}</td><td>{diagnostic.line?<button type="button" className="action-button secondary" onClick={()=>{setView('editor');setFocusLine(diagnostic.line);}}>{diagnostic.line}</button>:'—'}</td><td>{diagnostic.column??'—'}</td><td style={{whiteSpace:'normal'}}>{diagnostic.message}</td></tr>)}</tbody></table></div>:<div className="notice">No compiler diagnostics.</div>}<div className={`status-line ${webgl2&&!diagnostics.some((diagnostic)=>diagnostic.severity==='error')?'good':''}`} role="status">{status}</div></div></>;
}
