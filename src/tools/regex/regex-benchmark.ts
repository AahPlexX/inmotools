export interface RegexBenchmarkSummary { readonly iterations:number; readonly timeouts:number; readonly minMs:number; readonly medianMs:number; readonly p95Ms:number; readonly maxMs:number; readonly throughputPerSecond:number; }
const percentile=(sorted:readonly number[],p:number)=>sorted.length?sorted[Math.min(sorted.length-1,Math.max(0,Math.ceil(p*sorted.length)-1))]!:0;
export const buildBenchmarkSummary=(samples:readonly number[],iterations=samples.length,timeouts=0):RegexBenchmarkSummary=>{ const sorted=[...samples].filter(Number.isFinite).sort((a,b)=>a-b); const total=sorted.reduce((sum,value)=>sum+value,0); return {iterations,timeouts,minMs:sorted[0]??0,medianMs:percentile(sorted,.5),p95Ms:percentile(sorted,.95),maxMs:sorted.at(-1)??0,throughputPerSecond:total>0?(sorted.length*1000)/total:0}; };
