import type { AcademyCase, AcademyTrack } from './regex-types';
export interface RegexPracticeChallenge { readonly id:string; readonly title:string; readonly objective:string; readonly starter:string; readonly flags:string; readonly hint:string; readonly cases:readonly AcademyCase[]; }
export const buildPracticeChallenge=(tracks:readonly AcademyTrack[],seed=0):RegexPracticeChallenge=>{const lessons=tracks.flatMap((track)=>track.lessons);const lesson=lessons[lessons.length?Math.abs(Math.trunc(seed))%lessons.length:0]!;return{id:`practice-${lesson.id}-${seed}`,title:`Practice: ${lesson.title}`,objective:lesson.objective,starter:lesson.starter,flags:lesson.flags,hint:lesson.hint,cases:lesson.cases};};
